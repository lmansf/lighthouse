<!-- Generated by scripts/generate-reference.mjs. Do not edit manually. -->

# Function reference

This page is a big list of the jobs the code can do.

Each line has the code name first and then a short, simple clue about what that job helps the app do.

The jobs are grouped by file because a file is like one drawer that keeps similar jobs together.

## `app/api/chat/route.ts`

- `POST` - Lets one part of the app send information to another part.

## `app/api/connect/route.ts`

- `POST` - Lets one part of the app send information to another part.

## `app/api/diagnostics/route.ts`

- `GET` - Lets one part of the app ask another part for information.

## `app/api/model/route.ts`

- `DELETE` - Asks for something to be removed safely.
- `GET` - Lets one part of the app ask another part for information.
- `POST` - Lets one part of the app send information to another part.

## `app/api/open/route.ts`

- `POST` - Lets one part of the app send information to another part.

## `app/api/profile/route.ts`

- `GET` - Lets one part of the app ask another part for information.
- `POST` - Lets one part of the app send information to another part.

## `app/api/rag/route.ts`

- `GET` - Lets one part of the app ask another part for information.
- `POST` - Lets one part of the app send information to another part.

## `app/api/reveal/route.ts`

- `POST` - Lets one part of the app send information to another part.

## `app/api/settings/route.ts`

- `GET` - Lets one part of the app ask another part for information.
- `POST` - Lets one part of the app send information to another part.

## `app/api/upload/route.ts`

- `POST` - Lets one part of the app send information to another part.

## `app/providers.tsx`

- `Providers` - Helps the app do its job.

## `native/crates/lighthouse-core/src/analytics.rs`

- `analytics_cue` - Helps the app do its job.
- `batches_to_csv` - Helps the app do its job.
- `batches_to_markdown` - Helps the app do its job.
- `certified_metrics` - Helps the app do its job.
- `chart_card` - Helps the app do its job.
- `chart_columns` - Helps the app do its job.
- `chart_spec_from_batches` - Helps the app do its job.
- `chart_spec_from_batches_directed` - Helps the app do its job.
- `commafy` - Helps the app do its job.
- `decide_chart` - Helps the app do its job.
- `detect_header_row` - Helps the app do its job.
- `expand_views_for_freshness` - Helps the app do its job.
- `extract_sql` - Helps the app do its job.
- `fact_sheet` - Helps the app do its job.
- `freshness_line` - Helps the app do its job.
- `guard_metric_expression` - Helps the app do its job.
- `guard_sql` - Helps the app do its job.
- `is_pdf` - Answers a yes-or-no question.
- `is_tabular` - Answers a yes-or-no question.
- `join_hints` - Helps the app do its job.
- `last_query_used` - Helps the app do its job.
- `meta_table_json` - Helps the app do its job.
- `multi_step_cue` - Helps the app do its job.
- `parse_chart_directive` - Turns writing into pieces the app can understand.
- `parse_step_reply` - Turns writing into pieces the app can understand.
- `propose_metric` - Helps the app do its job.
- `pruned_schema_card` - Helps the app do its job.
- `question_tokens` - Helps the app do its job.
- `rank_tables` - Helps the app do its job.
- `reconcile_metric` - Helps the app do its job.
- `register_tables` - Helps the app do its job.
- `register_views` - Helps the app do its job.
- `row_cap_footer` - Helps the app do its job.
- `run_direct` - Does the job this helper was made for.
- `run_direct_save` - Does the job this helper was made for.
- `run_query` - Does the job this helper was made for.
- `sanitize_table_name` - Helps the app do its job.
- `saved_age_label` - Helps the app do its job.
- `sql_mentions_table` - Helps the app do its job.
- `sql_question` - Helps the app do its job.
- `sql_question_for` - Helps the app do its job.
- `step_question` - Helps the app do its job.
- `truncation_footer` - Helps the app do its job.
- `union_groups` - Helps the app do its job.
- `union_stem` - Helps the app do its job.
- `unique_table_name` - Helps the app do its job.
- `unregistered_count` - Helps the app do its job.
- `validate_directive` - Checks that something is safe and makes sense.

## `native/crates/lighthouse-core/src/answer_cache.rs`

- `cache_key` - Helps the app do its job.
- `candidate_digest` - Helps the app do its job.
- `insert` - Helps the app do its job.
- `key_from_parts` - Helps the app do its job.
- `lookup` - Helps the app do its job.
- `mined_analytics_sqls` - Helps the app do its job.
- `normalize_question` - Helps the app do its job.
- `reset_store` - Helps the app do its job.

## `native/crates/lighthouse-core/src/ask.rs`

- `run_headless_ask` - Does the job this helper was made for.

## `native/crates/lighthouse-core/src/audit.rs`

- `append` - Helps the app do its job.
- `ask_new_cost` - Helps the app do its job.
- `enabled` - Helps the app do its job.
- `export_csv` - Makes a copy that can be shared or saved elsewhere.
- `recent` - Helps the app do its job.
- `verify` - Helps the app do its job.
- `verify_active` - Helps the app do its job.

## `native/crates/lighthouse-core/src/beam.rs`

- `plan_only_bypasses_cache` - Helps the app do its job.
- `step_one_plan` - Helps the app do its job.

## `native/crates/lighthouse-core/src/boards.rs`

- `create` - Helps the app do its job.
- `default_board_id` - Helps the app do its job.
- `delete` - Asks for something to be removed safely.
- `list` - Helps the app do its job.
- `list_for` - Makes a list so people can see what is there.
- `parse_cards` - Turns writing into pieces the app can understand.
- `refresh_cards` - Helps the app do its job.
- `rename` - Helps the app do its job.
- `set_cards` - Changes one saved choice.

## `native/crates/lighthouse-core/src/briefings.rs`

- `add` - Helps the app do its job.
- `compose_briefing_note` - Helps the app do its job.
- `due` - Helps the app do its job.
- `last_note_ms` - Helps the app do its job.
- `list` - Helps the app do its job.
- `mark_note_run` - Helps the app do its job.
- `note_due` - Helps the app do its job.
- `remove` - Helps the app do its job.
- `render_markdown` - Draws information so a person can see it.
- `run` - Does the job this helper was made for.

## `native/crates/lighthouse-core/src/budget.rs`

- `doc_segment_budget` - Helps the app do its job.
- `estimate_tokens` - Helps the app do its job.
- `input_char_budget` - Helps the app do its job.
- `input_token_budget` - Helps the app do its job.
- `output_reserve` - Helps the app do its job.
- `overflow_retry_verdict` - Helps the app do its job.
- `plan_keep` - Helps the app do its job.
- `resolve_tier` - Helps the app do its job.
- `resolve_tier_with` - Helps the app do its job.
- `segment_budgets` - Helps the app do its job.

## `native/crates/lighthouse-core/src/catalog.rs`

- `columns_for` - Helps the app do its job.

## `native/crates/lighthouse-core/src/config.rs`

- `app_state_dir` - Helps the app do its job.
- `app_version` - Helps the app do its job.
- `connectors_dir` - Helps the app do its job.
- `is_desktop_app` - Answers a yes-or-no question.
- `iso_now` - Helps the app do its job.
- `now_ms` - Helps the app do its job.
- `parse_ms` - Turns writing into pieces the app can understand.
- `platform_kind` - Helps the app do its job.
- `profile_path` - Helps the app do its job.
- `read_json` - Reads saved information.
- `resources_dir` - Helps the app do its job.
- `sharepoint_authority` - Helps the app do its job.
- `sharepoint_client_id` - Helps the app do its job.
- `state_dir` - Helps the app do its job.
- `state_path` - Helps the app do its job.
- `utc_day` - Helps the app do its job.
- `vault_dir` - Helps the app do its job.
- `write_json` - Saves information safely.
- `write_json_compact` - Saves information safely.

## `native/crates/lighthouse-core/src/egress.rs`

- `host_counts` - Helps the app do its job.
- `hosts_since` - Helps the app do its job.
- `record` - Writes down what happened so it is not forgotten.
- `reset_for_tests` - Helps the app do its job.
- `snapshot` - Helps the app do its job.

## `native/crates/lighthouse-core/src/embed.rs`

- `bundled_draft_model` - Helps the app do its job.
- `bundled_embed_model` - Helps the app do its job.
- `cosine_qf` - Helps the app do its job.
- `embed_texts` - Helps the app do its job.
- `hybrid_scores` - Helps the app do its job.
- `nudge_warm` - Helps the app do its job.
- `quantize` - Helps the app do its job.
- `query_vector` - Helps the app do its job.
- `rrf_scores` - Helps the app do its job.
- `semantic_enabled` - Helps the app do its job.
- `vectors_for` - Helps the app do its job.

## `native/crates/lighthouse-core/src/extract.rs`

- `cell_text` - Helps the app do its job.
- `excel_serial_to_iso` - Helps the app do its job.
- `extract_rich_text` - Helps the app do its job.
- `is_rich_file` - Answers a yes-or-no question.
- `ocr_could_apply` - Helps the app do its job.
- `text_is_ocr_derived` - Helps the app do its job.

## `native/crates/lighthouse-core/src/index.rs`

- `entries_for` - Helps the app do its job.
- `flush_now` - Helps the app do its job.
- `index_max_file_bytes` - Helps the app do its job.
- `invalidate_all` - Helps the app do its job.
- `invalidate_ids` - Helps the app do its job.
- `key_of` - Helps the app do its job.
- `max_query_chunks` - Helps the app do its job.
- `peek_entry` - Helps the app do its job.
- `snapshot_chunks` - Helps the app do its job.

## `native/crates/lighthouse-core/src/insights.rs`

- `scan` - Helps the app do its job.

## `native/crates/lighthouse-core/src/inspect.rs`

- `inspect` - Helps the app do its job.
- `inspect_view` - Helps the app do its job.

## `native/crates/lighthouse-core/src/investigations.rs`

- `add_conversation_ref` - Puts something into the right place.
- `create` - Helps the app do its job.
- `export_markdown` - Makes a copy that can be shared or saved elsewhere.
- `fork` - Helps the app do its job.
- `list` - Helps the app do its job.
- `listing` - Helps the app do its job.
- `notes_subdir` - Helps the app do its job.
- `rename` - Helps the app do its job.
- `resolve_ask_context` - Helps the app do its job.
- `resolve_scope_and_policy` - Helps the app do its job.
- `set_archived` - Changes one saved choice.
- `view` - Helps the app do its job.

## `native/crates/lighthouse-core/src/ledger.rs`

- `assumption_ledger` - Helps the app do its job.
- `assumption_ledger_parts` - Helps the app do its job.

## `native/crates/lighthouse-core/src/llm.rs`

- `cost_estimate_usd` - Helps the app do its job.
- `doc_segment_char_budget` - Helps the app do its job.
- `draft_answer` - Helps the app do its job.
- `full_doc_char_budget` - Helps the app do its job.
- `local_health` - Helps the app do its job.
- `max_doc_segments` - Helps the app do its job.
- `narration_tier` - Helps the app do its job.
- `remote_provider` - Helps the app do its job.
- `stream_answer` - Helps the app do its job.
- `system_prompt_for` - Helps the app do its job.
- `validate_key` - Checks that something is safe and makes sense.
- `warm_local_model` - Helps the app do its job.

## `native/crates/lighthouse-core/src/local_model.rs`

- `advertised_ctx` - Helps the app do its job.
- `advertised_llama_backend` - Helps the app do its job.
- `download_offer` - Helps the app do its job.
- `find_installed_model` - Looks for the right thing.
- `fm_overflow_count` - Helps the app do its job.
- `local_model_available` - Helps the app do its job.
- `local_model_supported` - Helps the app do its job.
- `model_gguf_files` - Helps the app do its job.
- `model_ops_allowed` - Helps the app do its job.
- `model_status` - Helps the app do its job.
- `models_dir` - Helps the app do its job.
- `note_fm_marker` - Helps the app do its job.
- `on_device_backend` - Helps the app do its job.
- `ops_allowed_here` - Helps the app do its job.
- `request_uninstall` - Helps the app do its job.
- `set_advertised_ctx` - Changes one saved choice.
- `set_advertised_llama_backend` - Changes one saved choice.
- `set_download_offer` - Changes one saved choice.
- `set_on_device_backend` - Changes one saved choice.
- `start_download` - Begins a job.
- `supported_here` - Helps the app do its job.
- `uninstall_marker_path` - Helps the app do its job.

## `native/crates/lighthouse-core/src/meta.rs`

- `applicable_recipes` - Helps the app do its job.
- `applicable_semantics` - Helps the app do its job.
- `capability_map` - Helps the app do its job.
- `counts_bar_spec` - Helps the app do its job.
- `meta_intent` - Helps the app do its job.
- `render_meta` - Draws information so a person can see it.
- `suggested_asks` - Helps the app do its job.
- `suggested_asks_resolved` - Helps the app do its job.
- `view_typed_columns` - Helps the app do its job.

## `native/crates/lighthouse-core/src/numguard.rs`

- `answer_has_unverified_number` - Helps the app do its job.
- `number_free_degradation` - Helps the app do its job.
- `number_tokens` - Helps the app do its job.
- `strip_citation_markers` - Helps the app do its job.
- `verified_set` - Helps the app do its job.
- `with_integer_parts` - Helps the app do its job.

## `native/crates/lighthouse-core/src/ocr.rs`

- `availability` - Helps the app do its job.
- `available` - Helps the app do its job.
- `enabled` - Helps the app do its job.
- `fit_edge` - Helps the app do its job.
- `keep_line` - Helps the app do its job.
- `recognize_image` - Helps the app do its job.
- `too_many_pixels` - Helps the app do its job.

## `native/crates/lighthouse-core/src/pdf_tables.rs`

- `detect_tables` - Helps the app do its job.
- `extract_tables` - Helps the app do its job.
- `is_numeric_cell` - Answers a yes-or-no question.
- `is_queryable_grid` - Answers a yes-or-no question.
- `queryable_tables` - Helps the app do its job.
- `tables_to_markdown` - Helps the app do its job.

## `native/crates/lighthouse-core/src/pins.rs`

- `add` - Helps the app do its job.
- `list` - Helps the app do its job.
- `list_for` - Makes a list so people can see what is there.
- `recheck_all` - Helps the app do its job.
- `recheck_one` - Helps the app do its job.
- `refresh_one` - Helps the app do its job.
- `remove` - Helps the app do its job.

## `native/crates/lighthouse-core/src/policy.rs`

- `audit_forced_on` - Helps the app do its job.
- `first_vault_root` - Helps the app do its job.
- `history_allowed` - Helps the app do its job.
- `hotkeys_allowed` - Helps the app do its job.
- `managed_error` - Helps the app do its job.
- `notifications_allowed` - Helps the app do its job.
- `ocr_allowed` - Helps the app do its job.
- `policy` - Helps the app do its job.
- `present` - Helps the app do its job.
- `provider_allowed` - Helps the app do its job.
- `reset_for_tests` - Helps the app do its job.
- `snapshot` - Helps the app do its job.
- `telemetry_allowed` - Helps the app do its job.
- `vault_path_allowed` - Helps the app do its job.

## `native/crates/lighthouse-core/src/profile.rs`

- `complete_onboarding` - Helps the app do its job.
- `effective_default_inclusion` - Helps the app do its job.
- `finish_mode` - Finishes a job and tidies up.
- `finish_vault` - Finishes a job and tidies up.
- `get_state` - Gets something the app needs.
- `local_model_config` - Helps the app do its job.
- `model_config` - Helps the app do its job.
- `resolved_key_for` - Helps the app do its job.
- `select_model` - Helps the app do its job.
- `set_default_inclusion` - Changes one saved choice.
- `sign_out` - Helps the app do its job.

## `native/crates/lighthouse-core/src/provider_auth.rs`

- `ensure_fresh_access` - Helps the app do its job.
- `poll_once` - Helps the app do its job.
- `signin_config` - Helps the app do its job.
- `signout` - Helps the app do its job.
- `start` - Begins a job.
- `status` - Helps the app do its job.
- `status_payload` - Helps the app do its job.

## `native/crates/lighthouse-core/src/quotes.rs`

- `digest_contexts` - Helps the app do its job.
- `split_sentences` - Helps the app do its job.

## `native/crates/lighthouse-core/src/recipes.rs`

- `lookup` - Helps the app do its job.
- `parse_recipe_cue` - Turns writing into pieces the app can understand.

## `native/crates/lighthouse-core/src/reports.rs`

- `assemble` - Helps the app do its job.
- `investigate` - Helps the app do its job.
- `investigate_templated` - Helps the app do its job.
- `list_reports` - Makes a list so people can see what is there.
- `read_note` - Reads saved information.
- `render_markdown` - Draws information so a person can see it.
- `write_report` - Saves information safely.

## `native/crates/lighthouse-core/src/secrets.rs`

- `derived_key` - Helps the app do its job.
- `get_provider_key` - Gets something the app needs.
- `set_provider_key` - Changes one saved choice.

## `native/crates/lighthouse-core/src/semantic.rs`

- `create_metric` - Makes something new.
- `create_metric_with_context` - Makes something new.
- `create_synonym` - Makes something new.
- `delete_metric` - Removes something that is no longer wanted.
- `delete_synonym` - Removes something that is no longer wanted.
- `eligible_for_posture` - Helps the app do its job.
- `list` - Helps the app do its job.
- `matched_subset` - Helps the app do its job.
- `metric_effectively_local_only` - Helps the app do its job.
- `planning_synonyms` - Helps the app do its job.
- `prompt_block` - Helps the app do its job.
- `prompt_block_matched` - Helps the app do its job.
- `propose_metrics` - Helps the app do its job.
- `propose_metrics_from_usage` - Helps the app do its job.
- `propose_synonyms` - Helps the app do its job.
- `rename_metric` - Gives something a new name.
- `resolve_metric` - Helps the app do its job.

## `native/crates/lighthouse-core/src/settings.rs`

- `read_desktop_settings` - Reads saved information.
- `set_appearance` - Changes one saved choice.
- `set_explorer_width` - Changes one saved choice.
- `set_flyout_width` - Changes one saved choice.
- `set_open_flyout` - Changes one saved choice.
- `set_openai_auth_method` - Changes one saved choice.
- `write_desktop_settings` - Saves information safely.

## `native/crates/lighthouse-core/src/sources/microsoft.rs`

- `disconnect` - Helps the app do its job.
- `download_item` - Helps the app do its job.
- `get_access_token` - Gets something the app needs.
- `is_connected` - Answers a yes-or-no question.
- `list_tree` - Makes a list so people can see what is there.
- `load_state` - Helps the app do its job.
- `mirror_dir` - Helps the app do its job.
- `poll_device_code` - Helps the app do its job.
- `save_state` - Helps the app do its job.
- `start_device_code` - Begins a job.

## `native/crates/lighthouse-core/src/sources/mod.rs`

- `add_reference` - Puts something into the right place.
- `add_rule` - Puts something into the right place.
- `create_folder` - Makes something new.
- `inspect` - Helps the app do its job.
- `list_nodes` - Makes a list so people can see what is there.
- `list_sources` - Makes a list so people can see what is there.
- `move_node` - Puts something in a new place.
- `remove_from_vault` - Takes something away safely.
- `remove_reference` - Takes something away safely.
- `remove_rule` - Takes something away safely.
- `rename_node` - Gives something a new name.
- `restore_from_vault` - Brings back something that was removed.
- `retrieve` - Helps the app do its job.
- `rules_listing` - Helps the app do its job.
- `set_included` - Changes one saved choice.
- `set_local_only` - Changes one saved choice.
- `set_source_available` - Changes one saved choice.

## `native/crates/lighthouse-core/src/sources/sharepoint.rs`

- `disconnect` - Helps the app do its job.
- `is_present` - Answers a yes-or-no question.
- `list_nodes` - Makes a list so people can see what is there.
- `owns_id` - Helps the app do its job.
- `refresh_listing` - Helps the app do its job.
- `retrieval_items` - Helps the app do its job.
- `set_available` - Changes one saved choice.
- `set_included` - Changes one saved choice.
- `source` - Helps the app do its job.

## `native/crates/lighthouse-core/src/sqlfmt.rs`

- `format_sql` - Helps the app do its job.

## `native/crates/lighthouse-core/src/synth.rs`

- `answer_pipeline` - Helps the app do its job.
- `cross_doc_cue` - Helps the app do its job.
- `dominant_doc` - Helps the app do its job.
- `is_cloud_provider` - Answers a yes-or-no question.
- `local_only_skip_note` - Helps the app do its job.
- `local_warm_wait` - Helps the app do its job.
- `map_question` - Helps the app do its job.
- `multi_file_span` - Helps the app do its job.
- `partition_segments` - Helps the app do its job.
- `rank_docs_from_hits` - Helps the app do its job.
- `recall_cue` - Helps the app do its job.
- `reduce_question` - Helps the app do its job.
- `reliability_blocks` - Helps the app do its job.
- `sample_segments` - Helps the app do its job.
- `strip_markers` - Helps the app do its job.
- `warm_wait_verdict` - Helps the app do its job.

## `native/crates/lighthouse-core/src/table_profile.rs`

- `fmt_num` - Helps the app do its job.
- `is_profileable` - Answers a yes-or-no question.
- `parse_delimited` - Turns writing into pieces the app can understand.
- `profile_answer` - Helps the app do its job.
- `profile_chart` - Helps the app do its job.
- `profile_column_names` - Helps the app do its job.
- `table_profile` - Helps the app do its job.

## `native/crates/lighthouse-core/src/updates.rs`

- `verify_update_signature` - Helps the app do its job.

## `native/crates/lighthouse-core/src/vault.rs`

- `active_included_file_ids` - Helps the app do its job.
- `add_file` - Puts something into the right place.
- `add_reference` - Puts something into the right place.
- `add_rule` - Puts something into the right place.
- `chunk_texts_named` - Helps the app do its job.
- `chunk_texts_of` - Helps the app do its job.
- `create_folder` - Makes something new.
- `doc_chunks` - Helps the app do its job.
- `doc_path` - Helps the app do its job.
- `doc_text` - Helps the app do its job.
- `enrich_rule` - Helps the app do its job.
- `inclusion_attribution` - Helps the app do its job.
- `invalidate_walk_cache` - Helps the app do its job.
- `is_effectively_included` - Answers a yes-or-no question.
- `is_effectively_local_only` - Answers a yes-or-no question.
- `list_nodes` - Makes a list so people can see what is there.
- `list_rules` - Makes a list so people can see what is there.
- `list_sources` - Makes a list so people can see what is there.
- `local_only_attribution` - Helps the app do its job.
- `local_only_subset` - Helps the app do its job.
- `move_node` - Puts something in a new place.
- `name_tokens_of` - Helps the app do its job.
- `named_but_excluded` - Helps the app do its job.
- `named_file_target` - Helps the app do its job.
- `node_is_local_only` - Helps the app do its job.
- `purge_conversation_notes` - Helps the app do its job.
- `read_text_abs_capped` - Reads saved information.
- `reference_roots` - Helps the app do its job.
- `reference_roots_with_ids` - Helps the app do its job.
- `refresh_artifact` - Helps the app do its job.
- `remove_from_vault` - Takes something away safely.
- `remove_reference` - Takes something away safely.
- `remove_rule` - Takes something away safely.
- `rename_node` - Gives something a new name.
- `resolve_node_path` - Helps the app do its job.
- `restore_from_vault` - Brings back something that was removed.
- `retrieve` - Helps the app do its job.
- `rule_display_name` - Helps the app do its job.
- `rules_listing` - Helps the app do its job.
- `set_included` - Changes one saved choice.
- `set_local_only` - Changes one saved choice.
- `set_source_available` - Changes one saved choice.
- `shareable_file_ids` - Helps the app do its job.
- `shareable_freshness_keys` - Helps the app do its job.
- `shareable_subset` - Helps the app do its job.
- `source_kind_of` - Helps the app do its job.
- `state_written_by_newer` - Helps the app do its job.
- `tokenize` - Helps the app do its job.
- `warm_index_async` - Helps the app do its job.
- `write_artifact` - Saves information safely.
- `write_conversation_note` - Saves information safely.

## `native/crates/lighthouse-core/src/vault_brief.rs`

- `draft_brief` - Helps the app do its job.
- `render_brief` - Draws information so a person can see it.

## `native/crates/lighthouse-core/src/views.rs`

- `collect_table_names` - Helps the app do its job.
- `create` - Helps the app do its job.
- `create_with_tables` - Makes something new.
- `delete` - Asks for something to be removed safely.
- `dependents_in` - Helps the app do its job.
- `dependents_of` - Helps the app do its job.
- `eligible_for_posture` - Helps the app do its job.
- `list` - Helps the app do its job.
- `normalize_view_name` - Helps the app do its job.
- `parse_shape_reply` - Turns writing into pieces the app can understand.
- `rename` - Helps the app do its job.
- `shape_fewshot_block` - Helps the app do its job.
- `shape_is_local_only` - Helps the app do its job.
- `shape_question` - Helps the app do its job.
- `shape_samples` - Helps the app do its job.
- `shape_view` - Helps the app do its job.
- `transitive_dependents` - Helps the app do its job.
- `transitive_dependents_in` - Helps the app do its job.
- `view_depth` - Helps the app do its job.
- `view_effectively_local_only` - Helps the app do its job.
- `would_cycle` - Helps the app do its job.

## `native/crates/lighthouse-core/src/watch.rs`

- `generation` - Helps the app do its job.
- `is_active` - Answers a yes-or-no question.
- `start` - Begins a job.

## `native/crates/lighthouse-core/tests/common/mod.rs`

- `lock_env` - Helps the app do its job.

## `native/crates/lighthouse-desktop/src/commands.rs`

- `add_paths` - Puts something into the right place.
- `chat_ask` - Helps the app do its job.
- `connect_op` - Helps the app do its job.
- `diag_report` - Helps the app do its job.
- `diagnostics` - Helps the app do its job.
- `model_download` - Helps the app do its job.
- `model_status` - Helps the app do its job.
- `model_uninstall` - Helps the app do its job.
- `open_explorer` - Opens something for the person using the app.
- `open_node` - Opens something for the person using the app.
- `open_vault_dir` - Opens something for the person using the app.
- `pick_link_paths` - Helps the app do its job.
- `private_model_availability` - Helps the app do its job.
- `profile_get` - Helps the app do its job.
- `profile_op` - Helps the app do its job.
- `rag_list` - Helps the app do its job.
- `rag_op` - Helps the app do its job.
- `reduce_transparency` - Helps the app do its job.
- `reveal_node` - Helps the app do its job.
- `settings_get` - Helps the app do its job.
- `settings_set` - Helps the app do its job.
- `show_main` - Helps the app do its job.
- `smoke_report` - Helps the app do its job.
- `update_now` - Brings saved information up to date.
- `update_state` - Brings saved information up to date.
- `upload_file` - Helps the app do its job.
- `watch_generation` - Helps the app do its job.
- `widget_hide` - Helps the app do its job.
- `widget_hold` - Helps the app do its job.
- `widget_resize` - Helps the app do its job.
- `widget_set_pin` - Helps the app do its job.
- `widget_show` - Helps the app do its job.

## `native/crates/lighthouse-desktop/src/desktop/mod.rs`

- `configure` - Helps the app do its job.
- `on_run_event` - Helps the app do its job.
- `setup` - Helps the app do its job.

## `native/crates/lighthouse-desktop/src/desktop/supervise.rs`

- `check_for_updates` - Looks carefully for a problem.
- `update_now` - Brings saved information up to date.
- `updater_pubkey` - Helps the app do its job.

## `native/crates/lighthouse-desktop/src/desktop/tray.rs`

- `build_app_menu` - Puts pieces together to make something useful.
- `handle_menu` - Helps the app do its job.
- `rebuild_tray_menu` - Helps the app do its job.

## `native/crates/lighthouse-desktop/src/desktop/widget.rs`

- `apply_autostart` - Helps the app do its job.
- `conserve_enabled` - Helps the app do its job.
- `ensure_widget_window` - Helps the app do its job.
- `hide_widget` - Helps the app do its job.
- `launched_by_autostart` - Helps the app do its job.
- `open_explorer` - Opens something for the person using the app.
- `register_summon_shortcut` - Helps the app do its job.
- `resume_servers` - Helps the app do its job.
- `reveal_with_os` - Helps the app do its job.
- `save_widget_pos` - Helps the app do its job.
- `set_widget_pinned` - Changes one saved choice.
- `set_widget_resident` - Changes one saved choice.
- `show_widget` - Helps the app do its job.
- `toggle_widget` - Helps the app do its job.
- `widget_held` - Helps the app do its job.
- `widget_mode` - Helps the app do its job.
- `widget_pinned` - Helps the app do its job.
- `widget_resident` - Helps the app do its job.

## `native/crates/lighthouse-desktop/src/lib.rs`

- `app_data_base` - Helps the app do its job.
- `has_bundled_ui` - Checks whether something is there.
- `main_window` - Helps the app do its job.
- `platform_kind` - Helps the app do its job.
- `read_settings` - Reads saved information.
- `run` - Does the job this helper was made for.
- `shell_log` - Helps the app do its job.
- `vault_dir_setting` - Helps the app do its job.
- `write_settings` - Saves information safely.

## `native/crates/lighthouse-mcp/src/protocol.rs`

- `handle` - Helps the app do its job.
- `parse_error` - Turns writing into pieces the app can understand.

## `native/crates/lighthouse-mcp/src/tools.rs`

- `call` - Helps the app do its job.
- `is_known` - Answers a yes-or-no question.
- `schemas` - Helps the app do its job.

## `native/crates/lighthouse-server/src/auth.rs`

- `is_same_origin` - Answers a yes-or-no question.

## `native/crates/lighthouse-server/src/lib.rs`

- `app` - Helps the app do its job.

## `native/crates/lighthouse-server/src/routes.rs`

- `chat_post` - Helps the app do its job.
- `connect_post` - Helps the app do its job.
- `diagnostics_get` - Helps the app do its job.
- `model_delete` - Helps the app do its job.
- `model_get` - Helps the app do its job.
- `model_post` - Helps the app do its job.
- `open_post` - Opens something for the person using the app.
- `profile_get` - Helps the app do its job.
- `profile_post` - Helps the app do its job.
- `rag_get` - Helps the app do its job.
- `rag_post` - Helps the app do its job.
- `settings_get` - Helps the app do its job.
- `settings_post` - Helps the app do its job.
- `upload_post` - Helps the app do its job.

## `native/crates/lighthouse-shell/src/boot_guard.rs`

- `begin` - Helps the app do its job.
- `clear_safe_mode` - Helps the app do its job.
- `mark_ready` - Helps the app do its job.
- `safe_mode` - Helps the app do its job.
- `sticky` - Helps the app do its job.

## `native/crates/lighthouse-shell/src/commands.rs`

- `add_paths` - Puts something into the right place.
- `connect_op` - Helps the app do its job.
- `diag_report` - Helps the app do its job.
- `err_string` - Helps the app do its job.
- `model_uninstall` - Helps the app do its job.
- `open_node` - Opens something for the person using the app.
- `percent_decode` - Helps the app do its job.
- `private_model_availability_impl` - Helps the app do its job.
- `private_model_verdict` - Helps the app do its job.
- `profile_get` - Helps the app do its job.
- `profile_op` - Helps the app do its job.
- `rag_list` - Helps the app do its job.
- `rag_op` - Helps the app do its job.
- `start_content_size_observer` - Begins a job.
- `start_content_size_observer` - Begins a job.
- `string_array` - Helps the app do its job.
- `watch_generation` - Helps the app do its job.

## `native/crates/lighthouse-shell/src/lib.rs`

- `open_with_os` - Opens something for the person using the app.

## `native/crates/lighthouse-shell/src/state_home.rs`

- `ensure_state_home` - Helps the app do its job.
- `legacy_state_dir` - Helps the app do its job.
- `mark_cache_no_backup` - Helps the app do its job.
- `mark_cache_no_backup` - Helps the app do its job.
- `state_home_verdict` - Helps the app do its job.

## `scripts/check-stamps.mjs`

- `checkStamps` - Looks carefully for a problem.
- `collectStamps` - Helps the app do its job.

## `src/contracts/mocks/auth.mock.ts`

- `subscribeAuth` - Helps the app do its job.

## `src/contracts/mocks/providers.ts`

- `modelProvidersFor` - Helps the app do its job.

## `src/contracts/onDeviceAvailability.ts`

- `privateModelVerdict` - Helps the app do its job.

## `src/contracts/real/auth.real.ts`

- `subscribeAuth` - Helps the app do its job.

## `src/contracts/types.ts`

- `runRecipeQuestion` - Does the job this helper was made for.

## `src/features/boards/BoardCard.tsx`

- `BoardCard` - Helps the app do its job.

## `src/features/boards/BoardPanel.tsx`

- `BoardHost` - Helps the app do its job.

## `src/features/boards/boardModel.ts`

- `appendCard` - Helps the app do its job.
- `cardFreshness` - Helps the app do its job.
- `detectStat` - Helps the app do its job.
- `formatCheckedRelative` - Helps the app do its job.
- `freshnessFromFooter` - Helps the app do its job.
- `moveCard` - Puts something in a new place.
- `reorderCard` - Helps the app do its job.
- `spanForSize` - Helps the app do its job.
- `statDelta` - Helps the app do its job.
- `withCardSize` - Helps the app do its job.
- `withoutCard` - Helps the app do its job.

## `src/features/boards/boardScope.ts`

- `addPinToCurrentBoard` - Puts something into the right place.
- `currentScopeBoard` - Helps the app do its job.

## `src/features/chat/AnalyticsChart.tsx`

- `AnalyticsChart` - Helps the app do its job.
- `standaloneChartSvg` - Helps the app do its job.

## `src/features/chat/AnswerReportAction.tsx`

- `AnswerReportAction` - Helps the app do its job.

## `src/features/chat/BriefingsPanel.tsx`

- `BriefingsPanel` - Helps the app do its job.

## `src/features/chat/ChatPanel.tsx`

- `ChatPanel` - Helps the app do its job.
- `computeAnchorScrollTop` - Helps the app do its job.

## `src/features/chat/HistoryNav.tsx`

- `HistoryNav` - Helps the app do its job.

## `src/features/chat/PinMiniChart.tsx`

- `PinMiniChart` - Helps the app do its job.

## `src/features/chat/ProviderSwitch.tsx`

- `ProviderSwitch` - Helps the app do its job.

## `src/features/chat/ReportChip.tsx`

- `ReportChip` - Helps the app do its job.

## `src/features/chat/ReportReaderHost.tsx`

- `ReportReaderHost` - Helps the app do its job.

## `src/features/chat/ReportsHome.tsx`

- `ReportsHome` - Helps the app do its job.
- `ReportsHomeHost` - Helps the app do its job.

## `src/features/chat/StatTile.tsx`

- `StatTile` - Helps the app do its job.

## `src/features/chat/SuggestionChips.tsx`

- `SuggestionChips` - Helps the app do its job.

## `src/features/chat/keyboardCenter.ts`

- `keyboardCenterVerdict` - Helps the app do its job.

## `src/features/chat/suggestionChipModel.ts`

- `mergeSuggestionChips` - Helps the app do its job.

## `src/features/chat/useValidatedChips.ts`

- `useValidatedChips` - Helps the app do its job.

## `src/features/egress/EgressShield.tsx`

- `EgressShield` - Helps the app do its job.
- `egressPillSummary` - Helps the app do its job.

## `src/features/explorer/FileExplorer.tsx`

- `FileExplorer` - Helps the app do its job.

## `src/features/explorer/FileInspector.tsx`

- `FileInspector` - Helps the app do its job.
- `FileInspectorHost` - Helps the app do its job.

## `src/features/explorer/FileTileGrid.tsx`

- `FileTileGrid` - Helps the app do its job.
- `FilesSurface` - Helps the app do its job.

## `src/features/explorer/FolderRulesDialog.tsx`

- `FolderRulesDialog` - Helps the app do its job.

## `src/features/explorer/flatten.ts`

- `flattenVisible` - Helps the app do its job.

## `src/features/feedback/BugReport.tsx`

- `BugReport` - Helps the app do its job.

## `src/features/feedback/FeedbackNudge.tsx`

- `FeedbackNudge` - Helps the app do its job.

## `src/features/feedback/nudgeVerdict.ts`

- `nudgeCalm` - Helps the app do its job.
- `nudgePresentVerdict` - Helps the app do its job.

## `src/features/help/FirstRunTour.tsx`

- `FirstRunTour` - Helps the app do its job.

## `src/features/help/tourGating.ts`

- `shouldAutoOpenTour` - Helps the app do its job.

## `src/features/investigations/InvestigationsNav.tsx`

- `InvestigationsNav` - Helps the app do its job.

## `src/features/localModel/LocalModelOption.tsx`

- `LocalModelInstallPanel` - Helps the app do its job.
- `LocalModelOption` - Helps the app do its job.
- `humanBytes` - Helps the app do its job.
- `useLocalModel` - Helps the app do its job.

## `src/features/onboarding/ModeChooser.tsx`

- `ModeChooserAuto` - Helps the app do its job.
- `modKey` - Helps the app do its job.
- `prettyShortcut` - Helps the app do its job.
- `showWidget` - Helps the app do its job.
- `summonHotkey` - Helps the app do its job.

## `src/features/onboarding/OnboardingPanel.tsx`

- `OnboardingPanel` - Helps the app do its job.

## `src/features/quickopen/QuickOpen.tsx`

- `QuickOpen` - Helps the app do its job.
- `emphasize` - Helps the app do its job.

## `src/features/semantic/DefineMetricDialog.tsx`

- `DefineMetricDialog` - Helps the app do its job.

## `src/features/semantic/SemanticNav.tsx`

- `SemanticNav` - Helps the app do its job.

## `src/features/settings/SettingsMenu.tsx`

- `AboutDialog` - Helps the app do its job.
- `AiModelsDialog` - Helps the app do its job.
- `AuditLogDialog` - Helps the app do its job.
- `PreferencesDialog` - Helps the app do its job.
- `SettingsMenu` - Helps the app do its job.

## `src/features/settings/SettingsPage.tsx`

- `SettingsPage` - Helps the app do its job.

## `src/features/startup/StartupPrompt.tsx`

- `StartupPrompt` - Begins a job.

## `src/features/update/UpdateNotice.tsx`

- `UpdateNotice` - Brings saved information up to date.

## `src/features/views/SaveViewDialog.tsx`

- `SaveViewDialog` - Helps the app do its job.

## `src/features/views/ShapeViewDialog.tsx`

- `ShapeViewDialog` - Helps the app do its job.

## `src/features/views/ViewInspector.tsx`

- `ViewInspector` - Helps the app do its job.
- `requestViewInspect` - Helps the app do its job.

## `src/features/views/ViewsNav.tsx`

- `ViewsNav` - Helps the app do its job.

## `src/features/widget/SummonHint.tsx`

- `SummonHint` - Helps the app do its job.

## `src/features/widget/WidgetBar.tsx`

- `WidgetBar` - Helps the app do its job.
- `invokeShell` - Helps the app do its job.

## `src/lib/answerTable.ts`

- `answerTable` - Helps the app do its job.
- `parseMarkdownTable` - Turns writing into pieces the app can understand.
- `parseTableJson` - Turns writing into pieces the app can understand.

## `src/lib/appearanceSpec.ts`

- `normalizeAppearance` - Helps the app do its job.
- `parseAppearanceDirective` - Turns writing into pieces the app can understand.
- `stripAppearanceRequestFences` - Helps the app do its job.

## `src/lib/askTypeahead.ts`

- `askSuggestions` - Helps the app do its job.
- `ghostCompletion` - Helps the app do its job.
- `lastAsk` - Helps the app do its job.

## `src/lib/billingNotes.ts`

- `apiKeyBillingNote` - Helps the app do its job.
- `signinBillingNote` - Helps the app do its job.

## `src/lib/chartFromTable.ts`

- `chartSpecFromTable` - Helps the app do its job.
- `hasEngineChartFence` - Checks whether something is there.
- `looksTemporal` - Helps the app do its job.

## `src/lib/chartSpec.ts`

- `detectGranularity` - Helps the app do its job.
- `formatGrouped` - Helps the app do its job.
- `formatTick` - Helps the app do its job.
- `formatXTick` - Helps the app do its job.
- `niceTicks` - Helps the app do its job.
- `parseChartDirective` - Turns writing into pieces the app can understand.
- `parseChartSpec` - Turns writing into pieces the app can understand.
- `scaleLinear` - Helps the app do its job.
- `stripChartFences` - Helps the app do its job.
- `stripChartRequestFences` - Helps the app do its job.
- `tableToCsv` - Helps the app do its job.
- `validateDirective` - Checks that something is safe and makes sense.

## `src/lib/citePreview.ts`

- `citationQuery` - Helps the app do its job.
- `citedChunkIndex` - Helps the app do its job.
- `requestFileInspect` - Helps the app do its job.

## `src/lib/collapseSections.ts`

- `remarkCollapseSections` - Helps the app do its job.

## `src/lib/evidencePack.ts`

- `answerMarkdownToHtml` - Helps the app do its job.
- `composeBoardPack` - Helps the app do its job.
- `composeEvidencePack` - Helps the app do its job.
- `composeReportHtml` - Helps the app do its job.
- `provenanceStampText` - Helps the app do its job.

## `src/lib/feedbackLinks.ts`

- `buildFeedbackClipboard` - Puts pieces together to make something useful.
- `buildFeedbackIssueUrl` - Puts pieces together to make something useful.
- `buildFeedbackMailto` - Puts pieces together to make something useful.
- `composeFeedbackBody` - Helps the app do its job.
- `feedbackKindLabel` - Helps the app do its job.

## `src/lib/historyGrouping.ts`

- `groupByRecency` - Helps the app do its job.
- `relativeTimeLabel` - Helps the app do its job.

## `src/lib/mentionQuery.ts`

- `activeMention` - Helps the app do its job.
- `replaceMention` - Helps the app do its job.

## `src/lib/openExternal.ts`

- `openExternal` - Opens something for the person using the app.

## `src/lib/openReport.ts`

- `openSavedReport` - Opens something for the person using the app.

## `src/lib/pinChart.ts`

- `parsePinNumber` - Turns writing into pieces the app can understand.
- `parsePinSummary` - Turns writing into pieces the app can understand.
- `pinChartData` - Helps the app do its job.

## `src/lib/privacyState.ts`

- `cloudProviderActive` - Helps the app do its job.
- `hiddenFromCloudCount` - Helps the app do its job.
- `hiddenFromCloudLabel` - Helps the app do its job.

## `src/lib/providerSwitch.ts`

- `shortProviderLabel` - Helps the app do its job.
- `switchArgs` - Helps the app do its job.
- `switchChoices` - Helps the app do its job.

## `src/lib/quickOpen.ts`

- `quickOpenMatches` - Helps the app do its job.

## `src/lib/recall.ts`

- `recallRelated` - Helps the app do its job.
- `recallTokens` - Helps the app do its job.

## `src/lib/refineChips.ts`

- `refineEligibility` - Helps the app do its job.

## `src/lib/reportExport.ts`

- `exportReportHtml` - Makes a copy that can be shared or saved elsewhere.
- `exportReportMarkdown` - Makes a copy that can be shared or saved elsewhere.
- `printReport` - Helps the app do its job.

## `src/lib/reportTargets.ts`

- `isSheetName` - Answers a yes-or-no question.
- `resolveSheetTable` - Helps the app do its job.

## `src/lib/sortTable.ts`

- `compareCells` - Helps the app do its job.
- `parseNumericCell` - Turns writing into pieces the app can understand.
- `sortRows` - Helps the app do its job.
- `truncationCaption` - Helps the app do its job.
- `truncationNoteFrom` - Helps the app do its job.

## `src/lib/sqlFormat.ts`

- `formatSql` - Helps the app do its job.

## `src/lib/sqlHighlight.ts`

- `tokenizeSqlDisplay` - Helps the app do its job.

## `src/lib/statRun.ts`

- `detectStatRun` - Helps the app do its job.

## `src/lib/statSpec.ts`

- `parseStatSpec` - Turns writing into pieces the app can understand.

## `src/lib/streamingMarkdown.ts`

- `safeMarkdownPrefix` - Helps the app do its job.
- `splitMarkdownBlocks` - Helps the app do its job.

## `src/lib/tableName.ts`

- `sanitizeTableName` - Helps the app do its job.
- `uniqueTableName` - Helps the app do its job.

## `src/lib/workingIndicator.ts`

- `reportStageLabel` - Helps the app do its job.
- `useDelayedFlag` - Helps the app do its job.
- `usePrefersReducedMotion` - Helps the app do its job.
- `useReportStageLabel` - Helps the app do its job.

## `src/server/answerCache.ts`

- `cacheKey` - Helps the app do its job.
- `candidateDigest` - Helps the app do its job.
- `insert` - Helps the app do its job.
- `keyFromParts` - Helps the app do its job.
- `lookup` - Helps the app do its job.
- `normalizeQuestion` - Helps the app do its job.
- `resetStore` - Helps the app do its job.

## `src/server/audit.ts`

- `appendAudit` - Helps the app do its job.
- `auditEnabled` - Helps the app do its job.
- `beginAudit` - Helps the app do its job.
- `exportCsvAudit` - Makes a copy that can be shared or saved elsewhere.
- `finishAudit` - Finishes a job and tidies up.
- `recentAudit` - Helps the app do its job.
- `resetAuditForTests` - Helps the app do its job.
- `verifyActiveAudit` - Helps the app do its job.

## `src/server/boards.ts`

- `createBoard` - Makes something new.
- `defaultBoardId` - Helps the app do its job.
- `deleteBoard` - Removes something that is no longer wanted.
- `listBoardRecords` - Makes a list so people can see what is there.
- `listBoards` - Makes a list so people can see what is there.
- `parseBoardCards` - Turns writing into pieces the app can understand.
- `refreshBoardCards` - Helps the app do its job.
- `renameBoard` - Gives something a new name.
- `setBoardCards` - Changes one saved choice.

## `src/server/briefings.ts`

- `addBriefing` - Puts something into the right place.
- `composeBriefingNote` - Helps the app do its job.
- `dueBriefings` - Helps the app do its job.
- `listBriefings` - Makes a list so people can see what is there.
- `removeBriefing` - Takes something away safely.
- `runBriefing` - Does the job this helper was made for.

## `src/server/budget.ts`

- `docSegmentBudget` - Helps the app do its job.
- `estimateTokens` - Helps the app do its job.
- `inputCharBudget` - Helps the app do its job.
- `inputTokenBudget` - Helps the app do its job.
- `isAppleFm` - Answers a yes-or-no question.
- `outputReserve` - Helps the app do its job.
- `overflowRetryVerdict` - Helps the app do its job.
- `parseTier` - Turns writing into pieces the app can understand.
- `planKeep` - Helps the app do its job.
- `resolveTier` - Helps the app do its job.
- `resolveTierWith` - Helps the app do its job.
- `segmentBudgets` - Helps the app do its job.
- `tierWindow` - Helps the app do its job.

## `src/server/config.ts`

- `appStateDir` - Helps the app do its job.
- `appVersion` - Helps the app do its job.
- `connectorsDir` - Helps the app do its job.
- `isDesktopApp` - Answers a yes-or-no question.
- `platformKind` - Helps the app do its job.
- `profilePath` - Helps the app do its job.
- `readJson` - Reads saved information.
- `resourcesDir` - Helps the app do its job.
- `stateDir` - Helps the app do its job.
- `statePath` - Helps the app do its job.
- `vaultDir` - Helps the app do its job.
- `writeJson` - Saves information safely.

## `src/server/egress.ts`

- `egressSnapshot` - Helps the app do its job.
- `hostCounts` - Helps the app do its job.
- `hostsSince` - Helps the app do its job.
- `recordEgress` - Writes down what happened so it is not forgotten.
- `resetEgressForTests` - Helps the app do its job.

## `src/server/extract.ts`

- `extractRichText` - Helps the app do its job.

## `src/server/http.ts`

- `isSameOrigin` - Answers a yes-or-no question.

## `src/server/inspect.ts`

- `inspect` - Helps the app do its job.

## `src/server/investigations.ts`

- `addInvestigationConversationRef` - Puts something into the right place.
- `createInvestigation` - Makes something new.
- `exportMarkdown` - Makes a copy that can be shared or saved elsewhere.
- `forkInvestigation` - Helps the app do its job.
- `investigationNotesSubdir` - Helps the app do its job.
- `investigationView` - Helps the app do its job.
- `investigationsListing` - Helps the app do its job.
- `listInvestigations` - Makes a list so people can see what is there.
- `renameInvestigation` - Gives something a new name.
- `resolveAskContext` - Helps the app do its job.
- `resolveScopeAndPolicy` - Helps the app do its job.
- `setInvestigationArchived` - Changes one saved choice.

## `src/server/llm.ts`

- `buildPrompt` - Puts pieces together to make something useful.
- `docSegmentCharBudget` - Helps the app do its job.
- `draftAnswer` - Helps the app do its job.
- `errorChain` - Helps the app do its job.
- `fullDocCharBudget` - Helps the app do its job.
- `healthUrlFor` - Helps the app do its job.
- `localHealth` - Helps the app do its job.
- `maxDocSegments` - Helps the app do its job.
- `narrationTier` - Helps the app do its job.
- `priorTurns` - Helps the app do its job.
- `systemPromptFor` - Helps the app do its job.
- `validateKey` - Checks that something is safe and makes sense.

## `src/server/localModel.ts`

- `advertisedCtx` - Helps the app do its job.
- `advertisedLlamaBackend` - Helps the app do its job.
- `downloadOffer` - Helps the app do its job.
- `isInstalled` - Answers a yes-or-no question.
- `localModelAvailable` - Helps the app do its job.
- `localModelSupported` - Helps the app do its job.
- `modelOpsAllowed` - Helps the app do its job.
- `modelStatus` - Helps the app do its job.
- `modelsDir` - Helps the app do its job.
- `onDeviceBackend` - Helps the app do its job.
- `requestUninstall` - Helps the app do its job.
- `setAdvertisedCtx` - Changes one saved choice.
- `setAdvertisedLlamaBackend` - Changes one saved choice.
- `setDownloadOffer` - Changes one saved choice.
- `setOnDeviceBackend` - Changes one saved choice.
- `startDownload` - Begins a job.

## `src/server/meta.ts`

- `countsBarSpec` - Helps the app do its job.
- `metaIntent` - Helps the app do its job.
- `renderMeta` - Draws information so a person can see it.
- `savedAgeLabel` - Helps the app do its job.

## `src/server/numguard.ts`

- `answerHasUnverifiedNumber` - Helps the app do its job.
- `numberFreeDegradation` - Helps the app do its job.
- `numberTokens` - Helps the app do its job.
- `stripCitationMarkers` - Helps the app do its job.
- `verifiedSet` - Helps the app do its job.
- `withIntegerParts` - Helps the app do its job.

## `src/server/pins.ts`

- `addPin` - Puts something into the right place.
- `listPins` - Makes a list so people can see what is there.
- `removePin` - Takes something away safely.

## `src/server/policy.ts`

- `auditForcedOn` - Helps the app do its job.
- `historyAllowed` - Helps the app do its job.
- `policyError` - Helps the app do its job.
- `policyPresent` - Helps the app do its job.
- `policySnapshot` - Helps the app do its job.
- `providerAllowed` - Helps the app do its job.
- `resetPolicyForTests` - Helps the app do its job.
- `telemetryAllowed` - Helps the app do its job.

## `src/server/profile.ts`

- `completeOnboarding` - Helps the app do its job.
- `defaultProviderFor` - Helps the app do its job.
- `effectiveDefaultInclusion` - Helps the app do its job.
- `finishMode` - Finishes a job and tidies up.
- `finishVault` - Finishes a job and tidies up.
- `getState` - Gets something the app needs.
- `localModelConfig` - Helps the app do its job.
- `modelConfig` - Helps the app do its job.
- `resolvedKeyFor` - Helps the app do its job.
- `selectModel` - Helps the app do its job.
- `setDefaultInclusion` - Changes one saved choice.
- `signOut` - Helps the app do its job.

## `src/server/quotes.ts`

- `digestContexts` - Helps the app do its job.
- `questionTokens` - Helps the app do its job.
- `splitSentences` - Helps the app do its job.

## `src/server/secrets.ts`

- `getProviderKey` - Gets something the app needs.
- `setProviderKey` - Changes one saved choice.

## `src/server/semantic.ts`

- `applicableSemantics` - Helps the app do its job.
- `createMetric` - Makes something new.
- `createMetricWithContext` - Makes something new.
- `createSynonym` - Makes something new.
- `deleteMetric` - Removes something that is no longer wanted.
- `deleteSynonym` - Removes something that is no longer wanted.
- `eligibleForPosture` - Helps the app do its job.
- `guardMetricExpression` - Helps the app do its job.
- `listSemantic` - Makes a list so people can see what is there.
- `metricEffectivelyLocalOnly` - Helps the app do its job.
- `promptBlock` - Helps the app do its job.
- `proposeSynonyms` - Helps the app do its job.
- `renameMetric` - Gives something a new name.
- `renderBlock` - Draws information so a person can see it.
- `resolveMetric` - Helps the app do its job.

## `src/server/settings.ts`

- `appearance` - Helps the app do its job.
- `explorerWidth` - Helps the app do its job.
- `flyoutWidth` - Helps the app do its job.
- `openFlyout` - Opens something for the person using the app.
- `readDesktopSettings` - Reads saved information.
- `setAppearance` - Changes one saved choice.
- `setExplorerWidth` - Changes one saved choice.
- `setFlyoutWidth` - Changes one saved choice.
- `setOpenFlyout` - Changes one saved choice.
- `writeDesktopSettings` - Saves information safely.

## `src/server/sources/microsoft/auth.ts`

- `disconnect` - Helps the app do its job.
- `getAccessToken` - Gets something the app needs.
- `isConnected` - Answers a yes-or-no question.
- `loadState` - Helps the app do its job.
- `mirrorDir` - Helps the app do its job.
- `pollDeviceCode` - Helps the app do its job.
- `saveState` - Helps the app do its job.
- `startDeviceCode` - Begins a job.

## `src/server/sources/microsoft/graph.ts`

- `downloadItem` - Helps the app do its job.
- `listTree` - Makes a list so people can see what is there.

## `src/server/sources/registry.ts`

- `addReference` - Puts something into the right place.
- `addRule` - Puts something into the right place.
- `createFolder` - Makes something new.
- `inspect` - Helps the app do its job.
- `listNodes` - Makes a list so people can see what is there.
- `listSources` - Makes a list so people can see what is there.
- `moveNode` - Puts something in a new place.
- `removeFromVault` - Takes something away safely.
- `removeReference` - Takes something away safely.
- `removeRule` - Takes something away safely.
- `renameNode` - Gives something a new name.
- `restoreFromVault` - Brings back something that was removed.
- `retrieve` - Helps the app do its job.
- `rulesListing` - Helps the app do its job.
- `setIncluded` - Changes one saved choice.
- `setLocalOnly` - Changes one saved choice.
- `setSourceAvailable` - Changes one saved choice.

## `src/server/sources/sharepoint.ts`

- `refreshListing` - Helps the app do its job.

## `src/server/synth.ts`

- `crossDocCue` - Helps the app do its job.
- `dominantDoc` - Helps the app do its job.
- `extractChartFence` - Helps the app do its job.
- `isCloudProvider` - Answers a yes-or-no question.
- `localOnlySkipNote` - Helps the app do its job.
- `mapQuestion` - Helps the app do its job.
- `multiFileSpan` - Helps the app do its job.
- `partitionSegments` - Helps the app do its job.
- `rankDocsFromHits` - Helps the app do its job.
- `reduceQuestion` - Helps the app do its job.
- `reliabilityBlocks` - Helps the app do its job.
- `sampleSegments` - Helps the app do its job.
- `warmWaitVerdict` - Helps the app do its job.
- `warmingLabel` - Helps the app do its job.

## `src/server/tableProfile.ts`

- `fmtNum` - Helps the app do its job.
- `isProfileable` - Answers a yes-or-no question.
- `parseDelimited` - Turns writing into pieces the app can understand.
- `profileAnswer` - Helps the app do its job.
- `profileChart` - Helps the app do its job.
- `tableProfile` - Helps the app do its job.

## `src/server/vault.ts`

- `activeIncludedFileIds` - Helps the app do its job.
- `addFile` - Puts something into the right place.
- `addReference` - Puts something into the right place.
- `addRule` - Puts something into the right place.
- `chunkTextsNamed` - Helps the app do its job.
- `createFolder` - Makes something new.
- `docChunks` - Helps the app do its job.
- `docText` - Helps the app do its job.
- `enrichRule` - Helps the app do its job.
- `inclusionAttribution` - Helps the app do its job.
- `isEffectivelyIncluded` - Answers a yes-or-no question.
- `isEffectivelyLocalOnly` - Answers a yes-or-no question.
- `listNodes` - Makes a list so people can see what is there.
- `listRules` - Makes a list so people can see what is there.
- `listSources` - Makes a list so people can see what is there.
- `localOnlyAttribution` - Helps the app do its job.
- `localOnlySubset` - Helps the app do its job.
- `moveNode` - Puts something in a new place.
- `namedButExcluded` - Helps the app do its job.
- `namedFileTarget` - Helps the app do its job.
- `pinnedNamedFile` - Helps the app do its job.
- `purgeConversationNotes` - Helps the app do its job.
- `readTextAbs` - Reads saved information.
- `recallCue` - Helps the app do its job.
- `refreshArtifact` - Helps the app do its job.
- `removeFromVault` - Takes something away safely.
- `removeReference` - Takes something away safely.
- `removeRule` - Takes something away safely.
- `renameNode` - Gives something a new name.
- `resolveNodePath` - Helps the app do its job.
- `restoreFromVault` - Brings back something that was removed.
- `retrieve` - Helps the app do its job.
- `ruleDisplayName` - Helps the app do its job.
- `rulesListing` - Helps the app do its job.
- `setIncluded` - Changes one saved choice.
- `setLocalOnly` - Changes one saved choice.
- `setSourceAvailable` - Changes one saved choice.
- `shareableFileIds` - Helps the app do its job.
- `shareableFreshnessKeys` - Helps the app do its job.
- `shareableSubset` - Helps the app do its job.
- `sourceKindOf` - Helps the app do its job.
- `stateWrittenByNewer` - Helps the app do its job.
- `writeArtifact` - Saves information safely.
- `writeConversationNote` - Saves information safely.

## `src/server/vaultBrief.ts`

- `fileKind` - Helps the app do its job.
- `renderBrief` - Draws information so a person can see it.

## `src/server/views.ts`

- `collectTableNames` - Helps the app do its job.
- `createView` - Makes something new.
- `createViewWithTables` - Makes something new.
- `deleteView` - Removes something that is no longer wanted.
- `dependentsOf` - Helps the app do its job.
- `eligibleForPosture` - Helps the app do its job.
- `guardViewSql` - Helps the app do its job.
- `inspectView` - Helps the app do its job.
- `listViews` - Makes a list so people can see what is there.
- `normalizeViewName` - Helps the app do its job.
- `renameView` - Gives something a new name.
- `transitiveDependents` - Helps the app do its job.
- `viewDepth` - Helps the app do its job.
- `viewEffectivelyLocalOnly` - Helps the app do its job.
- `wouldCycle` - Helps the app do its job.

## `src/shell/AppShell.tsx`

- `AppShell` - Helps the app do its job.

## `src/shell/CompactTabBar.tsx`

- `CompactTabBar` - Helps the app do its job.

## `src/shell/Sheet.tsx`

- `Sheet` - Helps the app do its job.
- `anySheetOpen` - Helps the app do its job.
- `useAnySheetOpen` - Helps the app do its job.

## `src/shell/Sidebar.tsx`

- `Sidebar` - Helps the app do its job.

## `src/shell/VersionBadge.tsx`

- `VersionBadge` - Helps the app do its job.

## `src/shell/compactTransition.ts`

- `compactPageLayers` - Helps the app do its job.

## `src/shell/controls/LhDialog.tsx`

- `LhDialogSurface` - Helps the app do its job.

## `src/shell/controls/LhMenu.tsx`

- `LhMenu` - Helps the app do its job.
- `LhMenuPopover` - Helps the app do its job.

## `src/shell/controls/LhSegmented.tsx`

- `LhSegmented` - Helps the app do its job.

## `src/shell/controls/LhSelect.tsx`

- `LhSelect` - Helps the app do its job.

## `src/shell/controls/LhSwitch.tsx`

- `LhSwitch` - Helps the app do its job.

## `src/shell/controls/sheetDismiss.ts`

- `sheetDragArms` - Helps the app do its job.
- `sheetDragDismisses` - Helps the app do its job.

## `src/shell/desktopBridge.ts`

- `desktopBridge` - Helps the app do its job.
- `isDesktopShell` - Answers a yes-or-no question.
- `isMobileShell` - Answers a yes-or-no question.
- `pathsForFiles` - Helps the app do its job.
- `platformKind` - Helps the app do its job.
- `rememberPlatform` - Helps the app do its job.

## `src/shell/dnd.ts`

- `parseDraggedFiles` - Turns writing into pieces the app can understand.
- `serializeDraggedFiles` - Helps the app do its job.

## `src/shell/haptics.ts`

- `impactLight` - Helps the app do its job.
- `selectionChanged` - Helps the app do its job.

## `src/shell/paneLayout.ts`

- `paneLayout` - Helps the app do its job.
- `useCoarsePointer` - Helps the app do its job.
- `useCompactViewport` - Helps the app do its job.
- `usePaneLayout` - Helps the app do its job.

## `src/shell/shellSignals.ts`

- `publishChatStreaming` - Helps the app do its job.
- `publishShellUi` - Helps the app do its job.
- `useShellUi` - Helps the app do its job.

## `src/shell/systemAppearance.ts`

- `readReduceTransparency` - Reads saved information.

## `src/shell/tauriTransport.ts`

- `installTauriTransport` - Helps the app do its job.

## `src/shell/theme.ts`

- `themeFor` - Helps the app do its job.

## `src/shell/useVaultTree.ts`

- `useVaultTree` - Helps the app do its job.

## `src/stores/managedLocks.ts`

- `chatHistoryLocked` - Helps the app do its job.
- `setManagedLocks` - Changes one saved choice.

## `src/stores/useChatStore.ts`

- `conversationsAllContexts` - Helps the app do its job.
- `conversationsForContext` - Helps the app do its job.
- `pruneByAge` - Helps the app do its job.

## `src/stores/useOnDeviceModel.ts`

- `onDeviceBackendReady` - Helps the app do its job.
- `useOnDeviceModel` - Helps the app do its job.
