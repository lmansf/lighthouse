<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Hardened-runtime entitlements for the Developer-ID-signed Tauri bundle
         (wired via tauri.conf.json bundle.macOS.entitlements; inert while
         builds are unsigned). JIT/executable memory: the WKWebview JS engine
         and llama.cpp's runtime codegen both want it. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- The app spawns bundled third-party binaries (llama-server, piper) and
         loads their dylibs, which are not signed by our identity; allow that. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
  </dict>
</plist>
