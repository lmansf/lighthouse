# Ruby tooling for mobile code-signing + store delivery (fastlane).
# Desktop/JS/Rust builds do NOT need this — it is only used by the iOS/Android
# release lanes (see fastlane/ and docs/mobile-signing.md).
source "https://rubygems.org"

gem "fastlane"
