# Tasks — the semantic layer (metrics + certified + trust)

## 1. The semantic model + store (semantic.rs ⇄ semantic.ts) — do first
- [x] 1.1 New `native/crates/lighthouse-core/src/semantic.rs` on the `views.rs` template: store at `state_dir()/semantic.json`, versioned envelope `{v:1, metrics, synonyms, entities, joinHints}`, `store_lock`, `parse_store`/`Loaded`/`load`/`list`/`save` with bak-on-write, stable ids (`metric-`/`entity-` + sha1(name\nexpression\ncreatedMs)[..12]), `normalize_view_name`-style name sanitization + case-insensitive uniqueness. Register `pub mod semantic;` in `lib.rs`.
- [x] 1.2 Record types: `Metric {id, name, expression, description, entity, reads: Reads, summary, created_ms}`, `Synonym {term, canonical}`, `Entity {name, table, key_columns, description}`, `JoinHint {left_entity, left_column, right_entity, right_column, description}` — camelCase wire, serde-strict (an out-of-whitelist field is malformed, not coerced — the `views.rs` `SummarySource` posture).
- [x] 1.3 Metric guard: `analytics::guard_metric_expression(expression, entity)` synthesizes `SELECT <expression> AS <name> FROM <entity>` and runs `guard_sql` (analytics.rs:1513); derive `reads` from the synthesized statement via the `views::collect_table_names` AST walk. Refuse a non-read-only / unparseable / unknown-entity definition with a human-readable reason, persisting nothing on refusal.
- [x] 1.4 Local-only propagation + posture: `metric_effectively_local_only` (the `views::view_effectively_local_only` walk over a definition's `reads`) and `semantic::eligible_for_posture(is_cloud)` returning the definitions usable under the ask's posture — every definition on device, non-local-only only on a cloud ask. This one function governs §2 prompt injection AND the §3-cache key.
- [x] 1.5 CRUD + lifecycle mirroring `views.rs`: `create_metric`/`create_synonym`/`create_entity`/`rename`/`delete` (a metric referenced by a synonym refuses rename while dependents exist, or cascades on explicit confirm — the `views::rename`/`delete` rule). Metric name may not shadow a column of its entity.
- [x] 1.6 `resolve_metric(name) -> Option<String>`: the model-free resolver returning a metric's stored expression (shared by §2/§3/§4/eval).
- [x] 1.7 NO setting added — the store is the state (the shaped-views precedent); `settings.rs` and `settings_test.rs` are UNTOUCHED (state this explicitly, the plan-approval §4.5 precedent).
- [x] 1.8 Twin PARITY: `src/server/semantic.ts` mirrors the store, ids, CRUD, validation, and local-only propagation byte-compatibly (the `views.ts` precedent); the definition guard is a conservative textual single-SELECT check and `reads` derivation a FROM/JOIN scan, both with `PARITY:` comments.
- [x] 1.9 Tests: `semantic.rs` `#[cfg(test)]` pure tests (envelope round-trip + camelCase byte contract, id stability, name normalization, guard accept/refuse, local-only propagation on synthetic graphs); `tests/semantic_test.rs` for the store scenarios (round trip, bak-on-write, lifecycle, sources-untouched) under the shared VAULT_DIR env lock (the `views_test.rs` idiom); `test/semantic.test.mjs` mirrors the normalization + validation tables.

## 2. Resolution into NL→SQL (semantic block feeds the analytics prompt)
- [x] 2.1 `semantic::prompt_block(is_cloud) -> Option<Ctx>`: render the posture-eligible metrics (`name = expression — description`), synonyms (`term → canonical`), entities (`name: table (key cols) — desc`), and curated join hints in a fixed, deterministic order; cap by count (newest-first, the `register_tables` slot-cap idiom) so it can never blow the 6144-token window. `None`/empty when nothing eligible.
- [x] 2.2 Inject at `synth.rs:1268-1284`: after the view cards and before/beside `join_hints`, push the semantic block into `sql_ctxs` so BOTH `sql_question` (single-query) and `step_question` (multi-step) see it. Zero definitions ⇒ empty block ⇒ every prompt string byte-identical to today (pin with a test).
- [x] 2.3 `SEMANTIC_FEWSHOTS: &[(&str, &str)]` — question→blessed-SQL pairs showing a metric reference expanding to its definition; a test validates every SELECT with `guard_sql` (the `SHAPE_FEWSHOTS`/`SQL_FEWSHOTS` validating-test precedent) and snapshot-pins the rendered block.
- [x] 2.4 Curated join hints render alongside `analytics::join_hints` and win when they name the same table pair (labeled curated vs heuristic).
- [x] 2.5 Twin PARITY: `semantic.ts` mirrors `prompt_block` label strings byte-identically for the ctxs it can assemble; the analytics injection is Rust-only (no analytics branch) with a `PARITY:` note.
- [x] 2.6 Tests: eligible metrics render into the block; a local-only metric is absent on a cloud posture; empty store ⇒ byte-identical prompt; few-shot SELECTs all guard-pass; `resolve_metric` round-trips.

## 3. S3 — certified answers (§3)
- [x] 3.1 `analytics::certified_metrics(sql, &defs) -> Vec<String>`: parse the executed SQL with `DFParser` (the `ledger.rs`/`guard_sql` parser), collect projection expressions, and for each eligible metric compare its parsed expression by normalized-AST equality (`Expr::to_string()`, the `ledger.rs` idiom). Returns the metric names the SQL verifiably computed. Model-free; under-reports rather than guesses on unparseable SQL.
- [x] 3.2 `contracts.rs`: add `AnalyticsMeta.certified: Option<Vec<String>>` (`#[serde(default, skip_serializing_if = "Option::is_none")]`); mirror in `src/contracts/types.ts`. Set it at the three `AnalyticsMeta` construction sites (`synth.rs:1103` multi-step, `:1657` multi-step, `:1864` single-query) from `certified_metrics`.
- [x] 3.3 Emit an engine `*Certified:* <metric>` footer line after Query-used / Computed-from / Assumptions (the deterministic footer order), never model text; twin emits none (PARITY — no analytics branch).
- [x] 3.4 Persist through the cache: `certified` rides `AnalyticsMeta`, which `CachedAnswer.analytics` stores; a replay's `..hit.analytics` re-emits it (confirm by test — a cached certified answer replays certified). No recompute.
- [x] 3.5 Tests: exact-match SQL certifies + names the metric; a `SUM(amount)` near-miss of `SUM(amount) FILTER(…)` is NOT certified; narration-present vs -absent yields the same mark; replay stays certified (`answer_cache_test` over the model-free meta path).

## 4. S2 — trust check (§4)
- [x] 4.1 `analytics::reconcile_metric(ctx, sql, result, metric, entity) -> TrustVerdict`: run §3's definition check, then RE-RUN the blessed definition via `run_query` (`SELECT <expression> AS <name> FROM <entity>` + the answer's own GROUP BY/WHERE so like compares to like) on the same `ctx` and compare the re-run scalar/`QueryResult.digest` to the answer's. Model-free at every step.
- [x] 4.2 `contracts.rs`: `TrustVerdict {certified: bool, reconciled: bool, metric: Option<String>, expected: Option<String>, got: Option<String>}` + `AnalyticsMeta.trust: Option<TrustVerdict>` (default/skip-none); mirror in `types.ts`. Set at the three `AnalyticsMeta` sites beside `certified`.
- [x] 4.3 Degradation: a re-run error/timeout ⇒ `reconciled: false` with the reason, never a fabricated pass; a non-metric answer ⇒ `certified: false`, no reconcile (honest "not certified", not a failure). The trust check never breaks the already-computed answer.
- [x] 4.4 Deterministic: the same `(sql, result, defs)` yields a byte-identical verdict (snapshot-tested like the ledger). Twin PARITY: reconciliation is Rust-only (DataFusion); `PARITY:` note both sides; `TrustVerdict` shape mirrored in `types.ts` (wire only).
- [x] 4.5 Tests: a matching answer reconciles (`certified:true, reconciled:true`); a hand-crafted answer whose number differs from the re-run definition is CAUGHT (`reconciled:false` with expected/got); an ad-hoc answer is `certified:false`; a re-run error degrades to `reconciled:false`; verdict byte-identical across two runs.

## 5. Answer-cache key + CACHE_VERSION lockstep (cross-cutting, from §3/§4)
- [x] 5.1 `answer_cache.rs` `key_from_parts`/`cache_key` (:148/:191): add a semantic-registry component — posture-eligible definitions as sorted `(name, expression)` pairs — joining the key as `\ns:` ONLY when non-empty (the `\nv:` view-registry / `\nr:` recall precedent), so a changed definition invalidates dependent entries and a zero-definition vault leaves every legacy key byte-identical. Pin against the literal material (the `view_registry_joins_the_key_only_when_non_empty` precedent); `answerCache.ts` mirror.
- [x] 5.2 CACHE_VERSION 11→12 across `native/.../extract.rs:1338`, `src/server/extract.ts:136`, and the assertion in `tests/extract_test.rs` (§3/§4 add `certified`/`trust` to `AnalyticsMeta` — a shared cached-answer wire-shape change; ts-twin.md rule 4, the beam-loop 10→11 precedent), with a v11→12 doc note on each. The additive-optional fields keep old entries readable; the bump follows the lockstep convention regardless.

## 6. Management UI + surfaces (engine lands before UI)
- [x] 6.1 `meta.rs` `applicable_semantics(included, is_cloud)` mirroring `applicable_recipes`/`suggested_asks_resolved`: the eligible metrics/synonyms for the current tables, posture-gated via `eligible_for_posture`; returns a card shape for the nav. Dispatch `op:"semantic"` (list/create-metric/create-synonym/rename/delete) + `op:"defineMetric"` (propose a metric from a Beam answer's SQL — the "Save as view" precedent) across `routes.rs` / `commands.rs` / `app/api/rag/route.ts`; twin returns the visible subset / honest `{available:false}` on compute (PARITY).
- [x] 6.2 Contracts/UI: semantic-layer types + `RagService` methods (+ real/mock); `src/features/semantic/SemanticNav.tsx` on the `ViewsNav` template (list + define metrics/synonyms, mounted beside `ViewsNav`/`RecipesNav`); a "Define as metric" chip in the answer chip row; a certified badge + trust verdict rendered on the answer card from `AnalyticsMeta.certified`/`.trust`.
- [x] 6.3 `docs/data-flows.md` MUST NOT grow — no new egress (the semantic block rides the prompt already sent to the configured provider; certification/reconciliation are on-device `run_query`; local-only definitions force the local path). `docs/ts-twin.md` Rust-only table gains a semantic-layer row (certification/reconciliation Rust-only).

## 7. Eval floor + verify + parity + gates (§6)
- [ ] 7.1 Semantic goldens in `examples/analytics_eval.rs` — model-free, CI-gated like the Beam floor: (a) `resolve_metric` returns a metric's definition; (b) a certified answer's SQL AST-equals the blessed definition (and a near-miss does not); (c) the trust check catches a mismatch (a hand-crafted number that differs from the re-run definition ⇒ `reconciled:false`). Deterministic — removing narration changes no verdict.
- [ ] 7.2 Verify: store round-trip / bak-on-write / lifecycle (from 1.9); prompt-injection + empty-store-byte-identical + few-shot-guard (from 2.6); certified exact/near-miss/replay (from 3.5); reconcile match/catch/degrade/determinism (from 4.5); cache-key `\ns:` join + legacy byte-identity (from 5.1).
- [ ] 7.3 Diff the twin literals for the `prompt_block` labels and the `*Certified:*` footer (byte-identical, ts-twin.md rule 2); confirm the Rust-only `PARITY:` comments (§3 certify, §4 reconcile, §2 injection) on both sides.
- [ ] 7.4 Full gates: `cd native && cargo test --workspace`; `npm run test`; `tsc --noEmit`; `next lint`; `release-smoke`; `analytics_eval` floor (semantic goldens green); `node scripts/openspec-validate.mjs --all`.
