# Tasks — automation (headless CLI + MCP server + investigation branch/export)

## 1. The shared ask chokepoint + audit invariant (ask.rs) — do first
- [x] 1.1 New `native/crates/lighthouse-core/src/ask.rs`: `run_headless_ask(question: String, included_ids: Vec<String>, history: Vec<ChatTurn>, opts: AskOpts) -> impl Stream<Item = ChatChunk>`, where `AskOpts { local: bool, vault: Option<PathBuf>, investigation_id: Option<String>, attachment_ids: Vec<String> }`. Register `pub mod ask;` in `lib.rs`.
- [x] 1.2 Wire the invariant EXACTLY as the transports do (routes.rs:1105-1161 / commands.rs:1016-1051): base cfg = `profile::model_config()`, or `profile::local_model_config()` when `opts.local`; `investigations::resolve_ask_context(opts.investigation_id, opts.attachment_ids, cfg)` → `(attachments, cfg, prefs)`; `audit::AnswerAudit::start(&question)` before the stream; `answer_pipeline(question, included_ids, attachments, history, cfg, CacheCtl{privacy-safe defaults}, PlanCtl::none, prefs)`; `.finish(&provider, final_files, artifacts, answer_cost)` on the final chunk with `answer_cost = audit::ask_new_cost(&meta)` (None on replay — no double-count).
- [x] 1.3 Most-restrictive-wins posture: `opts.local` OR a `local-only` investigation both force `local_model_config`; local-only-marked scope files stay readable. Provenance is READ from the final chunk's `ChunkMeta` (`origin`, `cost`), never recomputed.
- [x] 1.4 `opts.vault` sets the engine's vault/state override before the first read (the `VAULT_DIR`-style override the test harness uses; the `LIGHTHOUSE_SMOKE_STATE` precedent for setting a root before the engine reads it).
- [x] 1.5 DECISION (state it in code + design): do NOT retrofit `chat_post`/`chat_ask` onto the helper in v1 (they already wire the identical sequence; retrofitting the `async_stream!` bodies risks a streaming/`finish`-timing divergence). Document `run_headless_ask` as the CANONICAL path for new ask entries; a retrofit is a follow-on gated on a byte-identical streaming parity test.
- [x] 1.6 NO twin: the helper is Rust entry plumbing (the two TS transports wire the equivalent inline in `app/api/chat`). NO new `DesktopSettings` field — `settings.rs`/`settings_test.rs` UNTOUCHED. NO `CACHE_VERSION` change (same `answer_pipeline`, same `CachedAnswer` shape).
- [x] 1.7 Tests (`ask.rs` `#[cfg(test)]` + a store-touching `tests/ask_test.rs` under the shared VAULT_DIR env lock): a headless ask writes an audit record with the provider + egress delta; `--local`/local-only forces device (`origin == "device"`, no egress); a cache replay records 0 new cost; provenance fields equal the `ChunkMeta` stamp.

## 2. The `lighthouse-cli` crate (the first true headless ask)
- [x] 2.1 New `native/crates/lighthouse-cli` workspace member (add to `native/Cargo.toml` `members`), `[[bin]] name = "lighthouse"`. Depends on `lighthouse-core` + a small arg parser + `serde_json` ONLY — NO Tauri/webkit (builds in the dev container; picked up by `--workspace`).
- [x] 2.2 `ask "<question>"` drives `run_headless_ask`, streaming answer deltas to stdout; ALWAYS emits a provenance line from `ChunkMeta` (origin; provider-reported tokens + labeled dollar estimate, or "not reported"/`$0.00`; egress verdict). Exit 0 on a settled answer, non-zero on failure / no answer.
- [x] 2.3 Flags: `--local` (force `local_model_config`, zero-network); `--vault <path>` (opts.vault); `--json` (emit `{answer, provenance{origin,inputTokens,outputTokens,totalTokens,reported,costEstimateUsd,sourceFileCount}, references, analytics?}` — provenance present in JSON too); `--investigation <id>`; `--include <file-id>` (repeatable). A local-only scope/investigation forces device with no flag (engine-decided).
- [x] 2.4 `fork <id> --name "<new name>"` and `export <id>` subcommands over the §4 engine functions (`export --json` returns `{savedId, savedName}`).
- [x] 2.5 Tests: arg parsing (flags → `AskOpts`); a `--local --json` ask over a fixture vault emits a grounded answer + a device provenance object; a failed ask exits non-zero. (The live cloud path needs a key and is out of the no-network container — covered by the eval/smoke, §5/§6.)

## 3. The `lighthouse-mcp` crate (in-process, stdio, read-only-leaning)
- [x] 3.1 New `native/crates/lighthouse-mcp` workspace member (add to `members`), `[[bin]] name = "lighthouse-mcp"`. Depends on `lighthouse-core` + a JSON-RPC/stdio helper + `serde_json` ONLY — NO Tauri/webkit. In-process against `lighthouse-core`, NOT shelling `lighthouse-server`.
- [x] 3.2 MCP over stdio (JSON-RPC on stdin/stdout): initialize + tool listing + tool call. NO listening port in v1.
- [x] 3.3 Tools (small, read-only-leaning): `ask_vault(question, local?, investigation?, included_file_ids?)` over `run_headless_ask` (returns answer text + provenance + references + `analytics?`); `list_files()` → `vault::list_nodes`; `list_investigations()` → `investigations::listing`; `run_analytics_sql(sql, file_ids)` → `analytics::run_direct` (guarded read-only SELECT). NO mutating tool in v1 (create/rename/archive/fork/export/defineMetric/exportChat/upload/move all OUT).
- [x] 3.4 `ask_vault` inherits the chokepoint (audited + egress-attributed); `run_analytics_sql` that is not a read-only SELECT is refused by `guard_sql` (the guard is the boundary, not the MCP layer).
- [x] 3.5 Transport posture note (code + design): stdio-only v1 has no network-auth surface; a future port MUST bind loopback and adopt `auth.rs` (loopback-host allowlist + same-port origin + `LIGHTHOUSE_API_TOKEN`).
- [x] 3.6 NO twin: Rust entry plumbing (ts-twin row, §5). Tests: tool listing shape; `ask_vault` returns a grounded answer + provenance over a fixture vault (`--local`); `list_files`/`list_investigations` shapes; a non-SELECT `run_analytics_sql` is refused.

## 4. Investigation fork + export (engine, then op surface, then twin)
- [x] 4.1 `investigations::fork(id, new_name) -> Result<Investigation, String>`: under `store_lock`, load the parent and mint a FRESH record (new `created_ms`, new `investigation_id(name, created_ms)`, new `sanitize_folder_name(new_name)`) copying ONLY `scope_file_ids` + `provider_policy` + `conversation_refs`; NOT archived; `new_name` trimmed/non-empty/case-insensitively unique (the `create` rule). Derived membership (pins/notes) is NOT duplicated — document against `investigations.rs:4-10`.
- [x] 4.2 `investigations::export_markdown(id, titles: Option<&Map>) -> Result<String, String>`: render the `InvestigationView` (record + derived `pin_refs`/`note_refs`) + `conversation_refs` to standalone markdown reusing the `briefings::render_markdown` idiom (`briefings.rs:243`). List scope files (or "whole vault"), conversation refs by id (`title (id)` when the map supplies one), the pin list, the note list. NEVER embed transcripts (`investigations.rs:9-10`).
- [x] 4.3 Export WRITE via the `exportChat` precedent: `notes_subdir(id)` (write-artifact allowlist, re-validated at use) + `vault::write_artifact` — non-egress in-vault write; returns `{savedId, savedName}`.
- [x] 4.4 Op surface: `investigations` op gains `action:"fork"` and `action:"export"` across `routes.rs:129`, `commands.rs`, `app/api/rag/route.ts` (export composes render + `notes_subdir` + `write_artifact`). A "Branch"/"Export" affordance in the investigations nav lands AFTER the engine function (engine-before-UI).
- [x] 4.5 Twin PARITY: `src/server/investigations.ts` mirrors `fork` (id minting, uniqueness, structure-only copy) and `exportMarkdown` (byte-identical markdown render) — the store/view derivation is already twinned there. NO `CACHE_VERSION` bump; the `{v:1}` investigations envelope is unchanged (fork reuses existing `Investigation` fields).
- [x] 4.6 Tests: `tests/investigations_test.rs` (VAULT_DIR env lock) — fork copies scope/policy/refs and mints a fresh id + empty notes folder without touching the parent's members; fork name collision/blank refuses; export writes under the investigation's notes folder and its markdown lists structure + membership with NO transcript text; export of an unknown id / unusable folder refuses. `test/investigations.test.mjs` mirrors the fork + render tables.

## 5. CI (workspace pickup + a Rust-native headless smoke) + docs
- [x] 5.1 `native/Cargo.toml` `members` includes `crates/lighthouse-cli` + `crates/lighthouse-mcp`; confirm `cargo build --workspace` / `cargo test --workspace` (native.yml:36/38) build and test both with NO workflow edit.
- [x] 5.2 A Rust-NATIVE headless smoke in `release-smoke.yml` (the first smoke with NO webview): build `lighthouse` (release, same 3-OS job), point it at the seeded smoke-fixture vault, run `lighthouse ask "<fixture question>" --local --json`, assert the answer cites the fixture and quotes its content; exit code = verdict (the boot-smoke contract). Slots beside the existing webview boot smoke.
- [x] 5.3 `docs/ts-twin.md`: add a Rust-only row for the CLI/MCP binaries (Rust entry plumbing, like the desktop shell + headless server — no `src/server` twin; only `fork`/`export_markdown` mirror in `investigations.ts`).
- [x] 5.4 `docs/data-flows.md` §8 (audit): note that the headless ask (`lighthouse ask`, MCP `ask_vault`) honors the SAME egress ledger + audit record as an app ask, via the shared chokepoint. No NEW egress overall (reads + `run_analytics_sql` are on-device; `--local`/local-only force device; export is a non-egress in-vault write).

## 6. Eval floor + verify + parity + gates
- [ ] 6.1 A model-free CLI/ask golden (an `examples/`-style or test check, CI-gated): `run_headless_ask --local` over a fixture yields a grounded, deterministic answer + a device provenance whose fields equal the `ChunkMeta` stamp; determinism holds across two runs.
- [ ] 6.2 Verify: audit-record-written + local-forces-device + replay-0-cost (from 1.7); CLI arg→opts + `--local --json` grounded answer + non-zero-on-failure (from 2.5); MCP tool shapes + `ask_vault` grounded + non-SELECT refused (from 3.6); fork structure-only + export references-not-transcripts + allowlist write (from 4.6).
- [ ] 6.3 Diff the twin literals for `investigations.ts::fork` + `exportMarkdown` (byte-identical render, ts-twin.md rule 2); confirm the Rust-only `PARITY:` notes (the CLI/MCP binaries; the helper) and the ts-twin.md row.
- [ ] 6.4 Confirm NO version bump and NO `CACHE_VERSION` change (same `answer_pipeline`/`CachedAnswer`; `{v:1}` investigations envelope unchanged) — state it explicitly, the semantic-layer/beam-loop "no bump" precedent.
- [ ] 6.5 Full gates: `cd native && cargo build --workspace` (CLI + MCP compile in-container); `cargo test --workspace`; `npm run test`; `tsc --noEmit`; `next lint`; `release-smoke` (incl. the new Rust-native ask smoke); `analytics_eval`/`chart_eval` floors; `node scripts/openspec-validate.mjs --all`.
