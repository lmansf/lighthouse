# Tasks — quant depth (forecast + changepoint + band chart) + proactive insights

## 1. The `band` chart kind (shared grammar — do first; the forecast recipe draws it)
- [x] 1.1 Rust (`analytics.rs`): added `ChartDirectiveKind::Band` and, on the DIRECTIVE, `lower_column`/`upper_column: Option<String>` (band names the bound columns; directives are parsed not serialized, so no wire change). The band SPEC series carries `lower`/`upper` arrays (built in `chart_spec_from_batches_directed`). `validate_directive` requires exactly one series + both bound columns (existing, numeric) for a band, else invalid.
- [x] 1.2 `decide_chart` never AUTO-selects `band` (the heuristic `chart_spec_from_batches` is unchanged — bounds come only from an ENGINE-authored directive); an invalid `band` directive falls through to the heuristic (a plain line). REFINEMENT: `chart_card` is left unchanged — the model must NOT pick band (it can't name bound columns), so teaching it would waste budget and invite misuse.
- [x] 1.3 TS twin (`src/lib/chartSpec.ts`): mirrored byte-for-byte — `ChartKind`/`ChartDirectiveKind` gain `"band"`; the series type gains optional `lower?`/`upper?`; the directive gains `lowerColumn?`/`upperColumn?`; `parseChartSpec`/`parseChartDirective`/`validateDirective` match the Rust rules and error strings. PARITY notes added.
- [x] 1.4 Render (`src/features/chat/AnalyticsChart.tsx`): draws `band` as a line + a low-opacity filled interval between `lower`/`upper` over the contiguous run with both bounds (the forecast tail), theme-aware, reusing the axis/legend code; the Y domain includes the bounds.
- [x] 1.5 Tests with the code: Rust `directed_band_spec_carries_the_interval` (valid band materializes the sparse interval; bounds-less band is invalid → declines) — CI-gated via `cargo test --workspace`. Node mirror in `test/chartSpec.test.mjs` (band spec parse: bounds required + length-aligned + single-series + rejected off a band; band directive parse + validate messages). (The `chart_eval` example floor's band case folds into §2.3's forecast-draws-a-band assertion — the unit + node floors already gate the grammar.)

## 2. The `forecast` recipe (S4 — engine, then chart wiring, then eval)
- [x] 2.1 `recipes.rs`: `forecast` builtin + `plan_forecast` (needs Date + Numeric) — guarded SQL: period-indexed monthly series, closed-form OLS slope `b`/intercept `a`/residual σ via `SUM`/`COUNT`, `FORECAST_HORIZON` projected rows `value = a + b·t`, band `± Z_FORECAST·σ`; future month labels built by integer `/`,`%` on the decomposed `YYYY-MM` (DataFusion has no `printf`). Consts `Z_FORECAST = 1.96`, `FORECAST_HORIZON = 3`, `FORECAST_MIN_POINTS = 3`. Result columns `period, kind, value, lower, upper` + a fit-summary step.
- [x] 2.2 `narration_prompt` states trend direction/rate + the projected value in words, no fabricated number; stamps only the existing `AnalyticsMeta{sql, file_ids}`.
- [x] 2.3 Recipe-branch chart wiring: realized the `Option<fn>` intent as a `Recipe::chart(&self, res) -> Option<String>` method (id-dispatched → `forecast_band_chart`, `None` for the rest) — cleaner than a fn-pointer field in the `Copy` struct, no 7-literal edit. `forecast_band_chart` builds a `band` directive over the representative result (plan[0]: `period, kind, value, lower, upper`) via `chart_spec_from_batches_directed`. The recipe branch (`synth.rs`) emits the `lighthouse-chart` fence after the result tables when `recipe.chart(res)` is `Some`, mirroring the analytics-branch emit. VERIFIED: "forecast: draws a band chart over the projection" floor passes.
- [x] 2.4 Degradation: `n < 3` gated IN SQL (`WHERE f.n >= 3`) → zero forecast rows, history-only (narration says "not enough history"); `σ = 0` → band collapses to the line (lower == upper == value), still renders. No error path.
- [x] 2.5 Eval floor + tests: model-free `forecast` goldens in `analytics_eval.rs` (exact linear fixture — slope 100/intercept 0/σ 0 → projects 600/700/800 with collapsed band; 2-row fixture → zero forecast rows). Unit test `forecast_plans_history_and_fit_summary`. VERIFIED: 7 forecast checks pass.

## 3. The `changepoint-scan` recipe (S4 — engine, then eval)
- [x] 3.1 `recipes.rs`: `changepoint-scan` builtin + `plan_changepoint` (needs Date + Numeric) — guarded SQL scoring each interior split by `|μ_R − μ_L| / (STDDEV + ε)` via a cumulative prefix-sum window (`ROWS UNBOUNDED PRECEDING`), `ORDER BY score DESC LIMIT 1`. Consts `CHANGEPOINT_MIN_POINTS = 4`, `CHANGEPOINT_EPSILON = 1e-9`. Result `changepoint_period, mean_before, mean_after, delta, magnitude`.
- [x] 3.2 `narration_prompt` states where + how much the series shifted, using the engine means; stamps only `AnalyticsMeta{sql, file_ids}`.
- [x] 3.3 Degradation: `n < 4` gated IN SQL (`WHERE ... s.n >= 4`) → no rows (narration says "not enough history"); a flat series → magnitude 0.0 via the `+ ε` floor, honest "no material shift". No error path.
- [x] 3.4 Eval floor + tests: model-free `changepoint` goldens (step fixture 100×4→500×4 → period 2024-04, means 100/500, delta 400, mag 1.87; flat fixture → magnitude 0.0). Unit test `changepoint_plans_a_single_guarded_scan`. VERIFIED: 2 changepoint checks pass.

## 4. Proactive insights (S5 — engine, then op, then UI)
- [x] 4.1 NEW `native/crates/lighthouse-core/src/insights.rs`: `scan(files, is_cloud) -> Insights { findings, tables_scanned, tables_available }` running the cheap detectors (`anomaly-scan`, `top-movers`, `changepoint-scan`) over cataloged Date+Numeric tables via the SAME `register_tables` + `run_query` the recipe branch uses; `Insight { table, kind, headline (templated from engine numbers), magnitude, sql }`; ranked by a NORMALIZED cross-detector magnitude (z / pct÷100 / changepoint-mag) desc; `pub mod insights;` in `lib.rs`. Reuses the recipe planners — no new SQL primitive. VERIFIED: 3 extractor unit tests + 3 scan integration tests.
- [x] 4.2 Bounded + disclosed: cap at `INSIGHTS_MAX_TABLES=12` scanned + `INSIGHTS_MAX=8` returned; `Insights` carries `tables_scanned`/`tables_available` so the surface discloses a cap ("scanned N of M"), never a silent truncation. Zero-network (no provider call, headlines templated — no model in the loop; proven by the keyless integration test). Degrade per table: an unanalyzable/failing table is skipped (test: a text-only table is skipped while the anomaly still surfaces), never fatal; empty result = honest "nothing stands out" (test: a flat vault returns `[]`).
- [x] 4.3 Op surface: an `insights` op gathers the included TABULAR files (`vault::active_included_file_ids` + `doc_path` + `is_tabular`) and returns `insights::scan` (camelCase via `#[serde(rename_all)]`) — `routes.rs` (`Json`), `commands.rs` (`Ok(json!)`, mirrors routes.rs call-for-call), `app/api/rag/route.ts` (PARITY: returns `{findings:[], tablesScanned:0, tablesAvailable:0}`). VERIFIED: lighthouse-server builds; tsc clean.
- [x] 4.4 UI: a proactive `InsightsNav` "What stands out" panel mounted at the TOP of the sidebar nav group (visible without a click; the pinned RecipesNav→ViewsNav→InvestigationsNav→FileExplorer adjacencies intact). Renders the engine `headline` VERBATIM per finding + a kind badge + the source table, the "Scanned N of M tables" disclosure when capped, plus loading + honest-empty states. Computes on show + re-scans on the vault's tabular-file-identity change (no always-on poll). Contract mirrors `applicableRecipes` (types + service + real + mock). Theme-aware (Fluent tokens).
- [x] 4.5 Tests: Rust — 3 extractor unit tests + 3 `insights_test.rs` scan integration tests (ranked anomaly surfaced; quiet vault `[]`; unanalyzable table skipped; keyless ⇒ zero-egress by construction). TS — `standsOutNavUi.test.mjs` (11) exercises the mock scan + asserts the panel's headlines/badges/disclosure/empty/loading. The route.ts op-empty degradation is a tsc-verified constant (the `applicableRecipes` precedent — route arms aren't node-tested) covered by the panel's empty-state test.

## 5. CI + docs
- [x] 5.1 The new forecast/changepoint goldens ride the already-gated `analytics_eval` (`native.yml`, `--locked`); the `insights` module + `insights_test.rs` are picked up by `cargo build/test --workspace` with NO workflow edit. (The band grammar's CI floor is the `directed_band_spec` unit test + the §2.3 forecast-draws-a-band `analytics_eval` check, per §1.5.) Confirmed.
- [x] 5.2 `docs/ts-twin.md`: added a Rust-only row for the `forecast`/`changepoint-scan` recipes + `insights::scan` (recipe execution Rust-only; `insights` op returns an empty scan) and stated the `band` chart kind IS a real `chartSpec.ts` twin, not Rust-only.
- [x] 5.3 `docs/data-flows.md` §6: noted that `insights::scan` is pure on-device SQL with NO model in the loop (headlines templated from engine numbers) so it touches egress not at all; forecast/changepoint egress exactly as any recipe narration does; the band chart rides inline in the answer markdown — no new network path.

## 6. Verify + parity + gates + no-bump confirmation
- [x] 6.1 Confirmed NO `CACHE_VERSION` bump: recipes stamp only the existing `AnalyticsMeta{sql, file_ids}`, the band chart rides inline in markdown, and insights are computed on demand (not cached). `CACHE_VERSION` stays **12** across `extract.rs`/`extract.ts`/`extract_test.rs`; `contracts.rs`/`answer_cache.rs`/`extract.rs` are UNTOUCHED by this change (empty diff).
- [x] 6.2 Confirmed NO version bump: workspace `0.12.1` and `package.json` `0.12.1` unchanged (the five stamps do not move) — the A/B/C precedent.
- [x] 6.3 Parity: the `band` twin is byte-compatible (`analytics.rs` ⇄ `chartSpec.ts` — same kind, same optional `lower`/`upper`, same validate rules + error strings; pinned by node tests). Rust-only posture: the forecast/changepoint recipes ride the existing Rust-only recipe branch (the TS `recipes` op is `{available:false}`); `insights` op returns an empty scan in `route.ts`; both recorded in the ts-twin.md row.
- [x] 6.4 Full gates GREEN: `cargo build`/`cargo test` (core lib 280 + insights/ask/investigations integration + server 10 + cli 13 + mcp 10; desktop excluded in-container — its `insights` arm grep-verified against the identical `routes.rs` arm); `npm test` 421; `tsc --noEmit` clean; `next lint` clean; `analytics_eval` (forecast/changepoint/band) + `chart_eval` floors pass; `node scripts/openspec-validate.mjs --all` 33/33.
