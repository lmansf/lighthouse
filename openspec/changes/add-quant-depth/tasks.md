# Tasks — quant depth (forecast + changepoint + band chart) + proactive insights

## 1. The `band` chart kind (shared grammar — do first; the forecast recipe draws it)
- [ ] 1.1 Rust (`analytics.rs`): add `ChartDirectiveKind::Band` (`~2448`) and, on the chart-spec series shape, additive `lower: Option<Vec<f64>>` / `upper: Option<Vec<f64>>` with `#[serde(skip_serializing_if = "Option::is_none")]` so a non-band series serializes byte-identically. Teach `chart_spec_from_batches_directed` (`~2592`) + `parse_chart_directive` (`~2482`) + `validate_directive` (`~2549`) the band kind; `validate_directive` requires `lower`/`upper` present and length-equal to the primary `values` for `band`, else the directive is invalid.
- [ ] 1.2 `decide_chart` (`~2716`) never AUTO-selects `band` from the heuristic (bounds come only from a recipe-authored directive); an invalid `band` directive falls through to the heuristic (a plain line). Add one `band` line to `chart_card` (`~2778`) for completeness, noting the model does not choose it.
- [ ] 1.3 TS twin (`src/lib/chartSpec.ts`): mirror byte-for-byte — `ChartKind`/`ChartDirectiveKind` gain `"band"`; the series type gains optional `lower?`/`upper?`; `parseChartSpec`/`parseChartDirective`/`validateDirective` match the Rust rules. Update the `PARITY:` cross-references on both sides (`analytics.rs:2441/2481/2548`).
- [ ] 1.4 Render (`src/features/chat/AnalyticsChart.tsx`): draw `band` as a line + a low-opacity filled area between `lower`/`upper`, theme-aware (light + dark), reusing the existing axis/legend code.
- [ ] 1.5 Tests with the code: Rust unit tests for band parse/validate (valid band; missing bounds → invalid; mismatched length → invalid; a non-band spec is byte-unchanged). Node mirror in the chart-spec parity suite (`test/chartSpec*.test.mjs`) with a pinned `band` fixture. A `band` golden in `examples/chart_eval.rs` (a directed band spec validates + keeps card budget; a bounds-less band is rejected).

## 2. The `forecast` recipe (S4 — engine, then chart wiring, then eval)
- [ ] 2.1 `recipes.rs`: add a `forecast` builtin to `BUILTINS` (`~188`) — `plan_forecast(&ResolvedParams) -> Vec<PlannedQuery>` needing Date + Numeric (the `anomaly-scan` `Applicability`). Plan guarded SQL that (a) builds the period-indexed monthly series, (b) computes OLS slope `b`, intercept `a`, and residual σ via `SUM`/`COUNT` aggregates, (c) projects `FORECAST_HORIZON` periods with `value = a + b·t` and band `± Z_FORECAST·σ`. Name `Z_FORECAST = 1.96` and `FORECAST_HORIZON = 3` as engine constants (the anomaly-fence discipline). Result columns: `period, kind(actual|forecast), value, lower, upper`.
- [ ] 2.2 Add a `narration_prompt` telling the model to state the trend direction/rate and the projected next value IN WORDS, citing the engine figures, emitting no number. Stamp only the existing `AnalyticsMeta{sql, file_ids}` (no new field).
- [ ] 2.3 Recipe-branch chart wiring: add an optional `chart: Option<fn(&[QueryResult]) -> Option<String>>` to the `Recipe` struct (`recipes.rs:81`), `None` for the existing five. `forecast` sets it to build a `band` directive from its result table. Wire the recipe branch (`synth.rs` ~877–1153) to emit the `lighthouse-chart` fence when a recipe declares a chart (mirroring the analytics-branch emit at `synth.rs:1978`).
- [ ] 2.4 Degradation: `n < 3` → a plain "not enough history to forecast (need ≥ 3 periods)" answer over the available series (no projection); `σ = 0` → band collapses to the line, still renders. No error path.
- [ ] 2.5 Eval floor + tests: a model-free `forecast` golden in `examples/analytics_eval.rs` (a hand-computed linear fixture — known slope/intercept — asserting the engine's projected values + band bounds via `assert_row`/`assert_keyed`, the recipe-golden template `analytics_eval.rs:825`). Unit tests for the short-series + zero-residual degradations.

## 3. The `changepoint-scan` recipe (S4 — engine, then eval)
- [ ] 3.1 `recipes.rs`: add a `changepoint-scan` builtin — `plan_changepoint(&ResolvedParams) -> Vec<PlannedQuery>` needing Date + Numeric. Plan guarded SQL that scores each interior split by `|μ_R − μ_L| / (σ_pooled + ε)` via cumulative prefix/suffix window aggregates, then `ORDER BY score DESC LIMIT 1`. Result: `changepoint_period, mean_before, mean_after, delta, magnitude`.
- [ ] 3.2 `narration_prompt`: state where and by how much the series shifted, in words, using the engine means; no fabricated number. Stamp only `AnalyticsMeta{sql, file_ids}`.
- [ ] 3.3 Degradation: `n < 4` → a plain "not enough history to locate a shift" answer; a flat series → top split with near-zero engine magnitude + an honest "no material shift" narration. No error path.
- [ ] 3.4 Eval floor + tests: a model-free `changepoint` golden in `examples/analytics_eval.rs` (a step-function fixture — low then high — asserting the changepoint period + before/after means the engine computes). Unit tests for the short-series + flat-series degradations.

## 4. Proactive insights (S5 — engine, then op, then UI)
- [ ] 4.1 NEW `native/crates/lighthouse-core/src/insights.rs`: `scan(tables) -> Vec<Insight>` running the cheap detectors (`anomaly-scan`, `top-movers`, `changepoint-scan`) over cataloged Date+Numeric tables via `run_query`; `Insight { table, kind, headline (templated from engine numbers), magnitude, sql }`; ranked by `magnitude` desc; `pub mod insights;` in `lib.rs`. Reuses the recipe planners — no new SQL primitive.
- [ ] 4.2 Bounded + disclosed: cap at `INSIGHTS_MAX_TABLES` scanned + `INSIGHTS_MAX` returned (engine constants); disclose unscanned tables (a "…and N more not scanned" note), never a silent truncation. Zero-network (no provider call, headlines templated — no model in the loop). Degrade per table: an unanalyzable/failing table is skipped, never fatal; empty result = honest "nothing stands out."
- [ ] 4.3 Op surface: an `insights` op (`routes.rs`, `commands.rs`, `app/api/rag/route.ts`) returning the ranked findings. PARITY: the TS `insights` op returns `[]` with a `PARITY:` note in `src/server/synth.ts` (analytics is Rust-only) — an honest empty, never a fake finding.
- [ ] 4.4 UI (engine-before-UI): a proactive insights panel/card in the app that presents the findings WITHOUT a question; v1 computes on show (may reuse the existing debounced vault-change signal), no always-on loop. License/gating consistent with the other analytics surfaces.
- [ ] 4.5 Tests: `insights::scan` over a fixture vault returns a ranked anomaly finding; a quiet vault returns `[]`; the cap is enforced + disclosed; an unanalyzable table is skipped; the scan issues no provider call (zero-egress). A node test for the TS op's empty degradation.

## 5. CI + docs
- [ ] 5.1 The new `examples/analytics_eval.rs` (forecast + changepoint) and `examples/chart_eval.rs` (band) floors ride the already-gated evals (`native.yml:43/45`, both `--locked`); `cargo build/test --workspace` picks up the `insights` module + new tests with NO workflow edit. Confirm.
- [ ] 5.2 `docs/ts-twin.md`: extend the analytics Rust-only row to name the `forecast`/`changepoint-scan` recipes + `insights::scan` (Rust-only; TS `insights` op returns `[]`); confirm the `band` chart kind is a REAL twin (`chartSpec.ts`), not Rust-only.
- [ ] 5.3 `docs/data-flows.md`: note that `insights::scan` is on-device SQL over already-cataloged tables (zero network, NO new egress path); forecast/changepoint egress exactly as any recipe narration does under the configured provider (no new egress).

## 6. Verify + parity + gates + no-bump confirmation
- [ ] 6.1 Confirm NO `CACHE_VERSION` bump (recipes stamp only the existing `AnalyticsMeta{sql, file_ids}`; the band chart rides inline in markdown; insights are computed on demand, not cached) — `CACHE_VERSION` stays 12 across the three lockstep sites. State it explicitly.
- [ ] 6.2 Confirm NO version bump (H-suite phase; the five stamps do not move) — the A/B/C precedent.
- [ ] 6.3 Parity: diff the `band` chart twin literals (`analytics.rs` ⇄ `chartSpec.ts`, byte-compatible parse/validate); confirm the Rust-only `PARITY:` notes for forecast/changepoint/insights in `src/server/synth.ts` + the ts-twin.md row.
- [ ] 6.4 Full gates: `cd native && cargo build --workspace` (insights + recipes compile in-container); `cargo test --workspace` (recipes + insights + chart tests); `npm run test`; `tsc --noEmit`; `next lint`; `analytics_eval`/`chart_eval` floors (incl. the new forecast/changepoint/band goldens); `node scripts/openspec-validate.mjs --all`. Desktop `commands.rs` `insights` arm grep-verified against the in-container `routes.rs` arm (the CI-only desktop build).
